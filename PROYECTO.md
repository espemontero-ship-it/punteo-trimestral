# Punteo trimestral — hoja de proyecto

Este documento es la fuente de verdad del proyecto: qué es, qué decisiones se han tomado y por qué, y qué queda pendiente. Se actualiza cada vez que se toma una decisión de producto, alcance o diseño — no hace falta bucear en sesiones antiguas para reconstruirlo.

## Regla de trabajo (fija, no negociable)

**Se muestra antes de implementar. Se habla antes de implementar.** Esto aplica a cualquier cambio — código real, config, base de datos, y también mockups/maquetas.

"Mostrar" = describir o previsualizar **sin tocar ningún archivo**. "Implementar" = editar el archivo (aunque sea un mockup) y volver a publicarlo. Son dos pasos distintos y no se saltan: si la usuaria pide "muéstramelo", la respuesta es una descripción o boceto en palabras, nunca una edición de archivo — ni siquiera de un mockup. Solo se edita después de una confirmación explícita a esa descripción. Una idea a medio formular, o una petición que "parece clara", no es autorización para tocar un archivo.

**2026-08-20 — se retira el "mockup siempre" (regla del 2026-07-27) y se cambia por esto:**

- **Cambio visual:** se dice en dos líneas qué se va a tocar, se hace, se sube, y lo mira ella en la app. Pequeño y reversible.
- **No se mete nada que ella no haya pedido explícitamente.** Si aparece algo que hace falta, se dice y se espera.
- **Al subir, dos listas cortas: lo que va a ver y dónde, y lo que se ha tocado y no se ve.** Si no se sabe decir dónde mirarlo, va en la segunda.

El mockup se retira porque una maqueta fuera de su pantalla no se parece a la de verdad: el 2026-08-19 se hicieron cinco del mismo panel y una pantalla entera, se rechazaron todas, y el arreglo acabó siendo dos cosas pequeñas que no se habían maquetado. Las dos listas salen de que **la usuaria no lee código**: un diff o un nombre de archivo no le sirven para controlar nada, y por eso ese día se colaron un ancho, una columna movida de sitio y textos nuevos sin que hubiera forma de verlos venir.

**2026-07-28 — subir cambios sin pedir permiso cada vez.** Una vez un cambio está verificado (build limpio, y probado en local cuando es posible), se hace commit y push directamente, sin esperar a que la usuaria escriba "commit y push" cada vez. Motivo explícito: "cuantos menos oks te dé mejor, porque hay veces que no puedo hacerte caso" — minimizar cuántas veces necesita responder, no solo por comodidad. Esto **no** afecta a la regla de arriba (mostrar antes de implementar cambios de diseño/estructura/navegación) — sigue exactamente igual, esa validación es sobre qué se construye, esta es sobre cuándo se publica algo ya construido y verificado.

**2026-08-24 — sin comentarios en el código.** No se escriben comentarios en el código: ni crónica de averías ("el 22/8/2026 vació cuatro", "por ahí entraron 124,74 € dados por buenos"), ni historia de qué hacía antes, ni justificaciones de por qué se cambió algo. Eso es un historial de incidencias metido dentro del código fuente y no le sirve a nadie: la usuaria no lee código, y para lo demás están el git y las conversaciones. Si algo necesita explicación, va en este documento. Ese día se quitaron los 1.874 comentarios que había: 13.514 líneas pasaron a 11.376.

## Guía de diseño (fija, no negociable)

Acordada explícitamente durante el rediseño de Movimientos (2026-08-03) y aplicada de verdad en `app/globals.css` — este documento existe para que no se pierda ni se reinvente cada vez (ya pasó una vez: vivía solo en un archivo de plan de Claude Code que se sobreescribió sin querer al empezar el siguiente plan; a partir de ahora vive aquí, que no se sobreescribe).

- **Excepción única y deliberada al punto siguiente — el ámbar de la sugerencia dudosa (decidida 2026-08-18).** La app propone dos cosas distintas con el mismo gesto: un cruce firme (mismo apellido, mismo importe) y un cruce del que ella misma duda (el banco solo escribe parte del nombre, o el importe no cuadra). Las dos se aceptan de un clic, así que confundirlas cuesta dinero — es literalmente el fallo que se pagó el 2026-08-18 con el pago de Nitzan Alon. Se probaron cuatro formas de distinguirlas sobre datos reales (borde discontinuo, borde sin palabra, la palabra "¿este?" sola, y ámbar) y la usuaria eligió el ámbar viéndolas las cuatro juntas. Vive en `--dudosa`/`--dudosa-soft` y **solo** se usa en `.sugerencia.dudosa`. No abre la puerta a más colores: cualquier otro caso sigue resolviéndose sin color.
- **Solo el color de acento (`--accent`) está permitido como color en toda la aplicación.** Ningún otro color (verde, rojo, ámbar...) se usa en ningún sitio nuevo — tampoco en hovers de acciones destructivas, eso se resuelve con negrita o subrayado, nunca con color. (Excepción histórica ya conocida y deliberadamente no tocada: `.etiqueta.fija/.nueva` en partes de la app no rediseñadas todavía — no es una plantilla a copiar en trabajo nuevo. `.etiqueta.mixta`/`--warn` se eliminaron el 2026-08-13 por no usarse en ningún componente real.)
- **El acento solo aparece en tres sitios, ninguno más:** la sugerencia del sistema, la pestaña activa de la cabecera, y el relleno del botón CTA principal de Inicio ("Subir ahora"). Ningún otro botón, tirador, hover ni icono lleva el acento — se resuelve con un gris más oscuro (`var(--text)`).
- **La sugerencia del sistema es UNA sola forma, para todos los tipos** (nota aprendida, proveedor, proyecto, devolución probable, jugador, cruce de LarpManager, factura de una subida en lote): una píldora `.sugerencia` de fondo `--accent-soft`, texto `--accent`, sin borde, con una **✕ dentro** al final. Pulsar el texto la acepta; pulsar la ✕ la rechaza. **Actualizado 2026-08-15**, sustituye al formato anterior "Aplicar: X" con la clase `.chip-sugerencia`, que ya no existe. Dos reglas que vienen con ella: **o la píldora o el campo, nunca las dos cosas a la vez** — cuando hay sugerencia se ve solo ella, y al rechazarla aparece el campo vacío como si nunca hubiera habido ninguna; y **a una línea ya resuelta no se le propone nada**, que su respuesta ya está dada.
- **Un enlace real** (abre algo, ej. ver una factura): color de texto normal (`var(--text)`) — nunca el acento. Dentro de una tabla, en negrita y sin subrayar (ver regla "sin cajas" más abajo); fuera de tabla, subrayado.
- **Un botón:** borde + fondo transparente casi siempre (clase `.secundario`, un único tamaño en toda la app — 13px, padding 7-11px); relleno de acento solo en el CTA de Inicio. No se crean tamaños de botón nuevos ni se deja ningún `<button>` sin clase (el `<button>` sin clase por defecto lleva el acento relleno a 16px, reservado solo para ese único CTA).
- **"Sin cajas" dentro de tablas (Movimientos, Facturas, Cuentas — decisión 2026-08-13):** ningún control de celda (`select`, botón `.secundario`, `.link-factura`, `.btn-editar-mini`) tiene caja permanente en reposo. La caja aparece igual para todos solo al pasar el ratón o hacer foco; en reposo lo único que distingue una acción de un dato es el peso de la letra (negrita), no un borde. Alcance explícitamente limitado a controles dentro de `.celda` — botones fuera de tabla (Login, diálogos, CTA de Inicio) conservan su caja permanente, no se han maquetado como "sin cajas".
- **Campos y botones pequeños dentro de una tabla:** 12-12.5px, sin excepciones sueltas por estilo en línea.
- **Espaciado:** escala fija 4/8/12/16/24px para huecos y márgenes entre bloques/filas. El padding interno de botones y campos es aparte, fijo por cada uno de los tamaños de arriba.
- **Sombra:** solo en tarjetas sueltas, nunca en una tabla.
- **Fondo de fila de una tabla:** solo cambia si hay un grupo real (más de una línea), nunca por decoración.
- **Toda lista de datos con columnas es una tabla de verdad**, con el mismo motor ya usado en Movimientos y Facturas (CSS Grid: `.tabla-movimientos-envoltura`/`.fila-tabla-cabecera`/`.fila-tabla`/`.celda`, cabecera con `resize-handle`) — nunca tarjetas apiladas ni filas sueltas en flexbox para una lista de registros, tampoco una lista de un solo enlace sin cabecera (aunque solo tenga una columna, ej. la lista de Proyectos).
- **La tabla nunca va envuelta en `.tarjeta`** — sin borde, sombra ni padding de card alrededor, siempre a ancho completo, exactamente como Movimientos y Facturas (modelo de referencia). Decisión 2026-08-13: Colaboradores, Cuentas y las tablas de la página de Proyecto llevaban una `.tarjeta` alrededor que las hacía ver distintas — se quitó, quedan desnudas como las demás. El título/descripción de la sección (ej. "Cuentas", "Devoluciones") va en texto plano encima, sin caja.
- **Cada columna es un único tipo de dato — no se mezclan conceptos distintos en la misma celda.** Si dos cosas son de naturaleza diferente (ej. "qué tipo de pago es" y "la nota de referencia", o "el estado" y "el motivo de rechazo"), van en columnas separadas, no una dentro de otra a base de texto secundario o saltos de línea.
- **Acciones raras de fila** (poco frecuentes, ej. editar una devolución, o quitar el vínculo de un pago de LarpManager): icono pequeño 20x20px, sin caja (ver "sin cajas" arriba), sin color ni siquiera en hover (`.btn-editar-mini`). El ✎ significa siempre "corregir algo que ya está puesto", y solo aparece en las filas que tienen ese algo — nunca como forma de crear nada nuevo. Los iconos "−" sacar del grupo y "+" unir a un grupo se retiraron el 2026-08-15: agrupar y desagrupar se hace con el campo Proveedor.
- **Acciones normales/frecuentes de fila** (vincular a mano, etc.): botones `.secundario` reales. Excepción confirmada: el Estado de una factura (revisar/aceptada/rechazada/cerrada/borrada) SÍ es un desplegable, no botones — pero nunca se guarda solo: elegir "rechazada" pide motivo, "cerrada" pide fecha, "borrada" pasa por `ConfirmDialog`, todo antes de persistir. El desplegable siempre refleja el último valor guardado, nunca un estado local sin confirmar (mismo patrón que "adelanto colaborador" en `TablaMovimientos.js`).
- **Confirmaciones de algo destructivo:** siempre `ConfirmDialog`, nunca `confirm()` nativo del navegador.
- **Formularios de "crear/añadir algo nuevo"** (colaborador, proyecto, lote, pago...): siempre visibles, nunca colapsados detrás de un `<details>` (el navegador dibuja su propio triángulo nativo si no se oculta explícitamente, y en esta app ese triángulo no existe en ningún sitio) — mismo tamaño de campos, mismo botón `.secundario` de envío, misma disposición en todos.
- **Tipografía: una sola, en toda la app, sin excepción.** `html, body` fija `Calibri, Candara, "Segoe UI", system-ui, sans-serif`, pero botones/inputs/selects no la heredan por defecto del navegador si no se fuerza — `button, input, select, textarea { font-family: inherit; }` en `app/globals.css` lo garantiza globalmente. No se resuelve caso a caso con `font: inherit` suelto en clases concretas (así se quedó desincronizado una vez, ver 2026-08-12).

## Qué es y para quién

Aplicación para una asociación sin ánimo de lucro (carpeta de Drive: "NOT ONLY LARP" — administración). Cada trimestre hay que mandar a la gestoría el punteo del banco contra las facturas de gastos, para que preparen IVA, IRPF e IS.

Proceso manual que sustituye: bajar el excel del banco, subirlo a Drive, buscar cada factura a mano y relacionarla por número (las facturas se numeran para que el nombre del archivo coincida con la nota escrita en el excel, ej. `59 y 60` cuando una línea del banco cubre dos facturas juntas).

## Alcance — decisiones cerradas

- **Ingresos y devoluciones de tickets quedan fuera del emparejamiento inteligente para siempre** (aprender de proveedor, cruzar contra una factura) — no del punteo en sí. **Matización 2026-07-27**: el punteo del banco cubre *todas* las líneas, ingreso o gasto, porque hay que dar cuenta de cada movimiento. Para una línea de ingreso, la "nota" no es un número de factura — es una referencia a su fila en la pestaña de ingresos/anticipos/devoluciones del mismo archivo que se manda a gestoría. El mecanismo ya existe: el campo de nota manual + "Confirmar" que ya tienen los grupos "nueva"/"mixta" sirve igual para esto, sin necesitar nada nuevo.
- **Sin IA/ML.** Todo determinista: regex + heurísticas sobre texto. Si algo falla, se puede ver exactamente por qué.
- **Sin OAuth de Google Drive.** Se lee una carpeta local sincronizada con Google Drive para escritorio — nunca se conecta la cuenta por API.
- **Agrupar por proveedor, no línea a línea.** Decisión explícita: revisar 380 líneas una a una, aunque cada una tenga una sugerencia, sigue siendo 380 decisiones — el ahorro real viene de actuar sobre el grupo entero de una vez.
- **El excel se sube cada vez, no hay ruta fija** — hay tres fuentes (bbva/openbank/paypal) que cambian cada trimestre.
- **Tiene que funcionar desde el móvil con cámara** (foto del recibo en el momento de comprar) — esto es lo que obligó a que fuera una webapp en la nube en vez de un script local.

## Ideas futuras (a pensar más adelante, no construir todavía)

- **Punteo de ingresos — hecho en parte (2026-08-01).** El cruce contra LarpManager (botón "Subir LarpManager", emparejamiento por nombre contra el CSV de pagos Wire) ya está construido — ver "Funcionalidades construidas". Quedan fuera de esta primera versión: enlace/escritura de vuelta a LarpManager, y buscar contra inscripciones pendientes sin confirmar en LarpManager. **Las devoluciones ya se construyeron por separado, ver más abajo.**
- ✅ **Devoluciones a jugadores — construido y verificado (2026-08-02).** Cuando un jugador se da de baja de un evento y se le devuelve parte de lo pagado (por PayPal o transferencia), esa línea saliente del banco se puede marcar como devolución en vez de como gasto de proveedor — mutuamente excluyente con Proveedor (`marcarDevolucion` en `lib/devoluciones.cjs`, quita el proveedor si lo hubiera y resuelve la línea directamente, sin desplegable de Estado intermedio). Diseño acordado explícitamente con la usuaria antes de construir (no hay export de bajas/cancelaciones en LarpManager con el que cruzar automáticamente, así que el nombre del jugador se escribe a mano, aunque se sugiere a partir del propio texto del banco):
  - **Cómo se marca — rediseñado (2026-08-02), tercera vuelta.** Pasó por tres versiones el mismo día: (1) un enlace visible en todas las filas → demasiado ruido en una tabla de cientos de gastos normales; (2) el enlace solo visible si el concepto sugería "devolución"/"refund"/"reembolso" → sin esa palabra no había forma de marcarla a mano; (3) **la que queda**: "devolución" es una opción más del desplegable de Estado (junto a pendiente/pedida/factura futura/ignorar/resuelta), siempre disponible en cualquier línea. Elegirla no guarda nada todavía — abre el campo de jugador en la celda de Proveedor (`modoDevolucion`); el desplegable, al ser un `<select>` controlado por el estado real guardado, vuelve solo a mostrar su valor normal en cuanto se re-renderiza. La sugerencia automática por texto sigue existiendo pero ya no es un elemento aparte: resalta el propio desplegable (mismo estilo que un campo prellenado) en vez de añadir un botón.
  - **Sugerir jugador**: se extrae del propio concepto ("TRANSFERENCIA A FAVOR DE X", "TRANSFERENCIAS X" de BBVA, o el nombre que ya encabeza el concepto de PayPal) — siempre editable antes de confirmar.
  - **Proyecto**: se reutiliza el desplegable de Proyecto que ya existía en la tabla, sin duplicar el campo.
  - **Por trimestre** (para el IVA): botón "Ver devoluciones" en la pestaña Trimestre + pestaña nueva "Devoluciones" en el excel final de "Cerrar trimestre" (Fecha/Importe/Proyecto/Jugador/Nota), solo si hay alguna ese trimestre.
  - **Por proyecto, cruzando trimestres** (para el cierre en IS): botón "Ver devoluciones" junto a cada proyecto en `/proyectos`, con descarga a CSV — un proyecto no vive dentro de un solo trimestre, así que esta vista consulta todos.
  - Verificado en local insertando un movimiento de prueba con "devolución" en el concepto: sugerencia de jugador correcta, confirmación pasa la línea a resuelta y quita el enlace de "sacar del grupo"/Proveedor normal, aparece en el modal del trimestre, en la pestaña del excel final (simulado, ya que Blob no funciona en local) y en el listado del proyecto tras asignarlo.
- ✅ **Estado "factura futura" — construido y verificado (2026-08-02).** Algunos proveedores (ej. DoYouSpain, Iberia) no emiten factura hasta que el servicio ha tenido lugar — perseguirla antes no tiene sentido, la factura literalmente no existe todavía. Diseño acordado explícitamente: estado nuevo y separado (no una variante de "pedida"), siempre sobre una línea del banco que ya existe (el pago ya salió), conviviendo con Proveedor y Proyecto igual que antes (a diferencia de Devolución, que sí los sustituye). El "recordatorio" no es una fecha suelta ni un email — está ligado al cierre del proyecto: la misma pantalla de `/proyectos` que ya muestra devoluciones ahora tiene también una sección "Facturas futuras sin recuperar" por proyecto, cruzando todos los trimestres.
  - Valor en BD `factura_futura` (sin migración de esquema — `estado` ya era `TEXT` sin `CHECK`, solo hacía falta que el código nuevo lo reconociera).
  - No cuenta como "pendiente" urgente: ni en el contador de la cabecera, ni en el "sin resolver" de cada grupo de proveedor, ni en la lista bajo "Solo pendientes" (se oculta igual que resuelta/ignorada) — está aparcada a propósito hasta que se cierre el proyecto, no es trabajo por hacer ahora mismo.
  - Verificado en local: contador de cabecera baja al marcarla, aparece correctamente en `/proyectos` tras asignar proyecto, vuelve a subir el contador al deshacerla.
- ✅ **Estado "ignorar" — construido y verificado (2026-08-02).** Segundo cambio pedido junto al anterior: una línea que no necesita ninguna acción (ni proveedor, ni nota, ni proyecto) se marca "ignorar" y desaparece de los pendientes — mismo tratamiento que "resuelta" en el filtro "Solo pendientes" y en los contadores, pero sin exigir nota ni factura. Quinta opción del desplegable de Estado, valor en BD `ignorada`.
- ✅ **Arreglado (2026-08-02) — Sugerencia de proyecto no reconocía el nombre sin el número.** `inferirProyecto` (`lib/proyectos.cjs`) solo sugería un proyecto si el concepto contenía su nombre completo tal cual ("Wield 2", "Inconscience 3") — en texto real del banco/PayPal casi nunca aparece el número de edición (ej. "refund Inconscience", "Vuelo Wield"), así que la sugerencia fallaba casi siempre y había que rellenar el proyecto a mano. Ahora también prueba el nombre sin el número final. Reportado por la usuaria al ver que tenía que rellenar proyecto y jugador a mano en una devolución cuyo texto ya los tenía los dos (el jugador sí se sugería bien; el proyecto no, por este bug).
- ✅ **Modelo continuo — construido (2026-08-02), migración de producción pendiente de confirmar.** La idea aparcada el 2026-08-01 ("ver todo el año, no solo un trimestre") se habló con calma y se construyó entera. Resumen de la conversación que lo motivó: "los trimestres solo existen de cara a la gestoría, iremos subiendo excels y facturas todo el tiempo, porque los proyectos son todo el año" — y el caso real que lo confirmó: una factura futura (ej. DoYouSpain/Iberia) que se recupera en el trimestre siguiente pero hay que reportarla como si fuera del anterior. Con trimestre como partición rígida de datos, eso no se podía hacer bien.
  - **Modelo de datos**: `movimientos` y `facturas` (las del flujo principal — no las de lote/colaboradores, ver más abajo) dejan de pertenecer a un trimestre. Viven en un único histórico continuo, identificados solo por su fecha real. `excels_originales` (un archivo por trimestre+banco, se reemplazaba al re-subir) se sustituye por `importaciones` (una fila por cada subida real, con su propia fecha) — necesario porque sin trimestre, subir el extracto de un banco varias veces al año son N archivos distintos en el tiempo, no uno que se pisa. `movimientos.importacion_id` sabe de qué archivo subido viene cada fila, para localizarla al generar el export final.
  - **"Cerrar trimestre" → "Generar envío a gestoría"**: ya no es una acción ligada a una partición de datos, es un paquete que cubre un rango de fechas, generado cuando haga falta. Tabla nueva `envios_gestoria` (etiqueta, desde, hasta, fecha de generación) + `envio_id` en movimientos/facturas (NULL = todavía sin enviar). Al generar un envío se incluye todo lo resuelto y sin enviar con fecha hasta el día elegido — **incluidas facturas recuperadas tarde de fechas anteriores**, que es justo el problema que lo motivó. Se puede volver a generar más adelante y solo se incluye lo nuevo desde el envío anterior.
  - **Bug real encontrado y arreglado durante la verificación**: el primer diseño marcaba los movimientos como enviados (`envio_id`) *antes* de generar el excel/zip final — si algo fallaba a mitad (ej. un blob que no se puede descargar), esos movimientos quedaban marcados como enviados sin haberse entregado de verdad, perdidos en silencio del siguiente envío. Arreglado: ahora el zip se genera primero, y solo si sale bien se marca todo como enviado. Verificado en local que un fallo a mitad ya no deja nada marcado (antes del fix sí lo dejaba; confirmado el bug de verdad, no solo en teoría).
  - **Navegación reestructurada a la vez** (mismo cambio, hablado y acordado en varias rondas): sin selector de trimestre como puerta de entrada — se entra directo a la app. Pestañas: **Inicio | Movimientos | Facturas | Proyectos | Colaboradores** (después se añadieron **Ayuda**, el 2026-08-13, y **LarpManager**, el 2026-08-16) (Admin desaparece: "Ver/borrar excels subidos" pasa a Movimientos, "Cerrar sesión" a la cabecera, "Nuevo colaborador" se queda con Colaboradores, "Proyectos" sube a pestaña propia). Cabecera compacta "Punteo" + Cerrar sesión a la derecha. La pestaña Movimientos gana un filtro de fechas (desde/hasta, opcional) y su barra de acciones se agrupa en *Subir* / *Revisar* / *Cierre* en vez de ocho botones sueltos en fila.
  - **Colaboradores NO se rediseñó** (se toca aparte, como módulo propio, más adelante) — sigue teniendo trimestre por lote tal cual. Como ya no hay un trimestre "actual" seleccionado en el resto de la app, la pantalla de Colaboradores ahora lista todos los lotes juntos (con su trimestre visible) y pide el trimestre a mano solo al crear un lote nuevo — es el único punto de fricción real que quedó de este cambio, aceptado a propósito para no meterse a rediseñar ese módulo ahora.
  - **Migración de datos real**: se hizo sobre una **rama de Neon aislada** (`dev`, creada el 2026-08-02 a partir de producción) — se descubrió a mitad de la conversación que `.env.local` y producción apuntaban a la misma base de datos de verdad (solo se estaba siendo cuidadosa de tocar nada más que el trimestre `test`), así que cualquier cambio de *esquema* (no solo de filas) habría afectado a datos reales sin esa rama de por medio. Los 226 movimientos reales existentes en el momento de migrar quedaron todos correctamente enlazados a su importación (verificado: 226/226, cero huérfanos).
  - **Verificado en local (rama `dev`) end-to-end**: las 5 pestañas cargan y funcionan (movimientos, cambiar proveedor de un grupo, facturas, proyectos con su modal de devoluciones/facturas futuras sin ya necesitar cruzar trimestres a mano, colaboradores con el nuevo selector de trimestre al crear lote), los modales de LarpManager sin emparejar/devoluciones/excels subidos, y la generación de envío (previsualización exacta, y confirmado que el bug de atomicidad de arriba está arreglado). **Lo único que no se pudo probar de principio a fin en local es que el .zip final se genere bien con datos reales** — Vercel Blob no funciona en local (limitación conocida de siempre, no un bug de este cambio), así que la descarga de los excels originales para escribir las notas falla con 403 aquí. La lógica es la misma que ya se usaba (y se verificó) para "cerrar trimestre" antes de este cambio, así que el riesgo es bajo, pero conviene probarlo de verdad en producción la primera vez que se genere un envío real.
  - ✅ **Aplicado en producción** (comprobado contra la base de datos real el 2026-08-16): `movimientos` ya no tiene columna `trimestre`, y `importaciones` y `envios_gestoria` existen. Quedan las tablas viejas `trimestres` y `excels_originales` sin usar — no molestan, pero ahí siguen.
  - ⚠️ **Todavía no se ha generado ni un solo envío real** (`envios_gestoria` vacía a 2026-08-16). Es decir: el paso final de todo el proceso — el zip con las facturas numeradas y el excel con las notas escritas — **nunca se ha ejecutado de verdad con datos de producción**. Es lo único grande de la app que sigue sin probarse en real, y el aviso de arriba sobre el `.zip` sigue vigente entero.

## Cómo funciona

No adivina proveedores por su cuenta: aprende de las notas ya escritas en trimestres anteriores. Agrupa por proveedor (no línea a línea). Al subir una factura, la cruza automáticamente contra las líneas pendientes de ese proveedor, incluida la detección de que dos facturas juntas sumen el importe de una línea.

El flujo real, tal como lo especificó la usuaria el 2026-07-27 (corrigiendo una lectura anterior errónea que mezclaba esto con "leer una carpeta de Drive"), tiene tres bloques separados. Importante para usabilidad — cada punto es un requisito propio, no una idea general:

### Antes de cerrar el trimestre

- Me ayudaría poder subir facturas sueltas o en grupo, para tenerlas listas para cuando vayamos a cerrar el trimestre.

### Cuando voy a cerrar el trimestre

- Subo excels para conciliación.
- Agrupamos por proveedor, para que tarde menos en la búsqueda.
- No subo de una en una — el fin es poder guardar todas las de un proveedor (ej. todas las de Amazon) y subirlas todas de golpe, y que el sistema haga el match.
- Necesito ver los proveedores que queden pendientes de buscar factura.
- Algunos proveedores me mandan la factura más tarde y se marcan pendientes. **Resuelto** (ver más abajo): atajo opcional "es una de las pendientes" al subirla.
- Cierro en determinado momento y mando la info a gestoría — necesito tener guardada en mi Drive toda la info.

### Gestión de proyectos concretos (va aparte de lo anterior)

- Crear colaborador.
- Que suba facturas.
- Aprobar facturas y hacer pagos y adelantos.

*(Esto último es la funcionalidad de colaboradores/lotes/pagos ya construida — confirmado que es una pista independiente del flujo de conciliación trimestral, no parte de él.)*

**Facturas que llegan tarde** — decisión tomada el 2026-07-27: al subir esa factura más tarde, hay un atajo **opcional** para decir "esto es una de mis pendientes" y elegirla de la propia lista de pedidas-esperando — match directo, sin depender de que el importe adivine bien. Es opcional y no el camino por defecto porque estos casos son raros (2-10 por trimestre) frente al volumen normal (200-500 líneas/trimestre) — forzar el paso extra siempre sería más fricción de la que ahorra.

## Stack técnico (y por qué, no solo qué)

**Next.js + Neon Postgres (vía integración de Vercel) + Vercel Blob + GitHub privado**, desplegado en Vercel.

Alternativas descartadas, con motivo real:
- **Script local en Python** — descartado porque no había Python instalado y porque el requisito de móvil+cámara obliga a un servidor accesible desde cualquier sitio, no solo el PC.
- **STRATO Hosting Profesional** (el hosting que ya tenía la asociación) — es hosting compartido, sin Node.js ni acceso SSH. Solo su plan VPS/Node.js lo permitiría, que es un producto distinto y de más coste.
- **Supabase** — su plan gratuito limita a 2 proyectos activos por organización, y esa organización ya estaba al límite con otras apps.

## Funcionalidades construidas, en orden

1. **Motor CLI** (`aprender`/`clasificar`/`matchear`/`puntear`) — aprende de las líneas ya punteadas históricamente (492 líneas la primera vez).
2. **Webapp**: checklist agrupado por proveedor, subida de factura con matching automático por importe (incluidas combinaciones de 2 facturas), estado "pedida, pendiente de recibir", todo persistido en base de datos (no en el navegador), captura de cámara en móvil, "Cerrar trimestre" genera zip de facturas numeradas + xlsx final.
3. Desplegado y verificado con datos reales: 291 grupos de proveedores (50 fijos, 201 factura propia, 31 nuevos), 310 combinaciones proveedor→nota migradas desde `memoria_proveedores.json`. **Ojo: esto fue verificación técnica/API (que los datos se procesan y guardan bien), no un uso real.** Nadie había probado a *usar* la app de verdad hasta después — fue al intentarlo cuando se vio que era un desastre en general, lo que disparó la auditoría de UX de más abajo.
4. Fix: subir el excel dos veces fusiona en vez de borrar las notas ya confirmadas. Las facturas sueltas se pueden subir antes de que exista el excel del trimestre.
5. Fix: bug de `pdf-parse` roto en el entorno serverless de Vercel (`DOMMatrix is not defined`) — solucionado bajando de v2 a v1.
6. Selector de trimestre (listar / crear / borrar) para poder probar sin ensuciar datos reales.
7. **Colaboradores, lotes y pagos**: la gente del equipo sube facturas de un evento a su propio "lote" (usuario/contraseña, no enlace mágico — se mezclan entre personas). El total gastado se ve en caliente desde la primera factura (control de gasto en curso, no solo al cierre). Cada factura se acepta o rechaza (ticket ≠ factura válida; las rechazadas nunca llegan a la gestoría). El pago se puede repartir en varios "pagos" independientes — un anticipo y su diferencia posterior son dos movimientos de banco distintos, no uno.

## Huecos identificados (2026-07-27, pendientes de construir)

- ✅ **Hecho — Subida en lote de facturas.** [SubirFacturasLote.js](app/components/SubirFacturasLote.js) (input `multiple`, procesa y empareja varias a la vez), ya en uso en la pestaña Trimestre. Esta nota se quedó desactualizada — comprobado 2026-08-01 que ya está construido, no es un hueco.
- **Atajo "es una de las pendientes"** al subir una factura que llegó tarde (ver flujo arriba) — diseño acordado, sin construir todavía.
- ✅ **Hecho (2026-07-27) — Información por línea en el checklist, como tabla real de columnas.** Cabecera del grupo: importe total del grupo, a la derecha. Al expandir: **tabla** de verdad (`<table>`, no tarjetas apiladas) con columnas Fecha / Concepto / Proveedor / Importe / Nota — una fila por movimiento (resuelto o no; antes los resueltos desaparecían del todo). Si ya tiene nota, se ve ahí mismo; si hay factura emparejada, enlace directo "Ver factura →". Backend: `lib/agrupador.cjs` también devuelve `factura_ids` por movimiento (join con `movimiento_facturas`). Verificado insertando datos de prueba en la base de datos local y comprobando en el navegador.
- ✅ **Hecho (2026-07-27) — Columna Proyecto.** Lista fija de proyectos (ej. "Wield 2"), creada una vez y reutilizada entre trimestres — **no está ligada a un trimestre ni a un año**, vive en su propio apartado `/proyectos`, enlazado desde el menú general (pantalla de selector de trimestre), no dentro de ningún trimestre. Al puntear una línea, si el concepto contiene el nombre de un proyecto ya creado se sugiere solo (botón "¿Wield 2?"), sin asignarlo hasta que se confirma — nunca decide sola. Estados de proyecto (activo/cerrado) quedan aparcados para cuando haga falta, no se construyen todavía. Tabla nueva `proyectos` + columna `movimientos.proyecto_id`, migración aplicada contra la base de datos real (`db/migration_proyectos.sql`). Verificado end-to-end en el navegador: crear proyecto, ver la sugerencia por texto, confirmarla, comprobar que queda guardada.
- Sobre el CLI (`aprender`/`clasificar`/`matchear`/`puntear`): **no se archiva ni se borra sin más.** La lógica de fondo (`lib/normalize.js`, `lib/clasificarCore.js`, `lib/facturas.js`, `lib/matchearCore.js`) ya es compartida con el webapp, así que no se pierde nada ahí. Lo único específico del CLI es que `matchear.cjs` procesa una carpeta entera de golpe — que es exactamente la misma necesidad que "subida en lote" de arriba, solo que resuelta del lado del CLI en vez del navegador. Construir la subida en lote en el webapp es lo que de verdad haría al CLI prescindible; hasta entonces, tocarlo sería quitar una capacidad real sin sustituto.

- ✅ **Hecho (2026-08-16) — Importación de LarpManager al mismo nivel que la del banco** (identificado 2026-08-14). Ver el detalle en el Registro de decisiones. Se mantiene abajo el diagnóstico original porque explica bien el problema:

  Las dos importaciones no se parecían en nada, y la de LarpManager estaba claramente peor construida: el excel del banco guarda el archivo original en almacenamiento, deja registro de cada subida (`importaciones`, con fecha y nombre de archivo), permite saber qué movimientos vinieron de cuál y **borrar una subida entera** desde "Excels subidos". El CSV de LarpManager no hace **nada** de eso: se procesa y se tira, no queda registro de la subida, no se puede saber qué pagos vinieron de cuál, y no hay forma de deshacer una subida equivocada. Además, al guardar solo conserva 4 campos (nombre, evento, importe, fecha) y **descarta `Method` e `Info`**, que son justo los que deciden si una fila entra o no — así que la decisión de qué se importó es irreversible y no se puede auditar después. Lo correcto: guardar todas las filas del CSV con todos sus campos (y el archivo original), y mover el filtro `Wire`/`manual` al momento de cruzar en vez de al de importar, para que la regla se pueda cambiar sin volver a subir nada. Detectado al no poder responder a "¿cuáles de estos pagos son transferencia?" sin pedirle el CSV a la usuaria — una pregunta que con el excel del banco se habría contestado sola. **Nota: esta migración NO arregla el bug del cruce de abajo**, son cosas distintas; se hace igualmente porque el almacenamiento actual es incorrecto.

## Bugs conocidos

- ✅ **Arreglado (2026-08-14) — El cruce de LarpManager ignoraba las líneas del banco ya resueltas, y por eso nunca las emparejaba.** `lib/larpmanager.cjs` busca los pagos del CSV solo entre los movimientos con `estado != 'resuelta' AND importe > 0`. Como la usuaria puntea a mano muchas transferencias **antes** de subir el CSV, esas líneas son invisibles para el cruce y sus pagos quedan en "Pagos sin emparejar" **para siempre**: no hay reintento que los recupere, y el problema empeora cuanto mejor y más al día lleve ella el punteo. Confirmado con datos reales de producción: de 12 pagos que llevaban semanas listados como "sin emparejar", **los 12 habían llegado al banco y sus 12 líneas estaban ya en `resuelta`** — no faltaba ni un euro. Arreglado quitando el filtro por estado: el cruce mira todos los ingresos, y cuando la línea ya está resuelta se limita a escribir `larpmanager_pagos.movimiento_id` sin tocarle el estado ni pintarle sugerencias (exigiendo que sea inequívoco: un único candidato y el importe cuadrando, porque ahí no hay a quién preguntar). **Aviso para el futuro: no confundir "pago sin emparejar" con "dinero que no ha llegado"** — antes de este arreglo esa lista medía otra cosa.
- ✅ **Arreglado (2026-08-14) — La normalización de nombres fallaba con los caracteres nórdicos, y además el banco es inconsistente consigo mismo.** El hallazgo importante, comprobado contra movimientos reales de producción: **el mismo banco transcribe la misma clase de carácter de dos formas distintas** — "Kaltenhäuser" lo escribió como `Kaltenhaeuser` (ä→ae) pero "Stöcklein" como `Stocklein` (ö→**o**). Es decir, no existe una transliteración única correcta, y la que había (siempre la larga: ä→ae, ö→oe, ü→ue) dejaba fuera la mitad de los casos. Aparte, `ø` no descompone con NFD (es un carácter propio, no una letra con acento), así que sobrevivía a `quitarAcentos` y el filtro `[^A-Z0-9 ]` lo convertía en un espacio: "Per Heegaard Jørgensen" quedaba como `PER HEEGAARD J RGENSEN` y el apellido buscado pasaba a ser `RGENSEN`, que en producción enganchó un pago de supermercado. Arreglado generando **las dos variantes** de cada nombre (`normalizarVariantes`, mapas `TRANSLITERACION_LARGA`/`TRANSLITERACION_CORTA`, con ø/å/æ/ð/þ añadidos) y aceptando que el banco haya escrito cualquiera de ellas. Verificado contra el CSV real: de 42 pagos, 5 generan dos formas, y `Stöcklein` produce `STOCKLEIN`, que es exactamente lo que el banco escribió.
- ✅ **Añadido (2026-08-14) — "Pagos sin emparejar" ahora dice POR QUÉ.** Antes esa lista era plana: un pago aparecía igual si la transferencia no había llegado, si el importe no cuadraba o si el nombre coincidía a medias, sin forma de distinguirlo — y era imposible saber si faltaba dinero o solo faltaba un enlace. Ahora cada fila lleva su motivo, calculado al vuelo contra los movimientos actuales (no guardado, para que refleje el banco de hoy y no el del día que se subió el CSV): `sin_confirmar` (encaja por apellido e importe), `importe_no_cuadra` (el apellido está pero por otra cantidad — caso real: Nick Ceccon, pago de 90€ contra una línea de 350€), `nombre_parcial` (coincide algo del nombre pero no el apellido; exige 4+ letras porque con 3 el "PER" de "Per Heegaard" enganchaba "SUPERMERCADOS") y `no_esta`.
- ✅ **Arreglado (2026-07-27) — Config de columnas de bbva desactualizada.** `config/sheets.json` tenía `importeCol: "H"` para bbva, pero el export real actual de bbva tiene `H` = **Saldo de la cuenta** (no el importe); el importe real está en `G`. Consecuencia doble: el importe se guardaba mal (saldo en vez de importe real), y como `textCols` incluía `G`, el número del importe se pegaba al texto del concepto (ej. "TRANSFERENCIAS ALVARO MAESTRO -51 64") — por eso la agrupación no consolidaba nada, cada fila tenía su propia "clave" única. Dos síntomas que parecían bugs de UI (agrupación rota, "faltan movimientos") eran el mismo bug de columnas. Corregido a `dataStartRow: 2, textCols: ["E","F"], importeCol: "G"` (columnas reales confirmadas con un excel real: `A` nº, `B` fecha contable, `C` fecha valor, `D` código, `E` concepto, `F` observaciones, `G` importe, `H` saldo, `I` divisa, `J` oficina — formato de export de bbva más nuevo que el usado al configurar `sheets.json` originalmente). Verificado simulando el parseo contra el excel real: 55 de 55 filas, importes limpios, "LIQUIDACION REMESA DE COMERCIOS" pasa de 17 tarjetas sueltas a 1 con 17 movimientos.
- ~~⚠️ **Dato importante:** los movimientos que ya existen en la base de datos de trimestres de prueba (ej. "test 2")~~ — **ya no aplica (2026-08-16):** los trimestres desaparecieron con el modelo continuo y en producción no queda ningún movimiento de aquellos (97 movimientos, todos reales). Se deja el texto porque explica un tipo de fallo que puede repetirse: los movimientos afectados se importaron con la config vieja y estaban corruptos (importe = saldo, texto con el importe pegado). Volver a subir el mismo excel a ese trimestre **no los corrige** — el import fusiona por fecha+importe+texto, y como esos valores cambian con el fix, se crearían filas duplicadas en vez de reemplazar las malas. Para probar limpio: borrar el trimestre de prueba y crear uno nuevo antes de volver a subir.
- ⚠️ **El formato de columnas de bbva no está cerrado todavía.** Existen (al menos) dos formatos reales distintos: el `2026.xlsx` que se entrega a gestoría (con columna extra "Beneficiario/Ordenante", importe en `H`, 2 filas de cabecera) y un export más nuevo sin esa columna (importe en `G`, 1 fila de cabecera) que se usó para el fix del 2026-07-27. La usuaria confirma que están todavía haciendo pruebas y que **el formato definitivo será otro distinto a estos dos** — la config actual de `sheets.json` vale para las pruebas de ahora, pero hay que revisarla otra vez en cuanto exista el formato final. No construir detección automática de formato hasta entonces, sería prematuro.
- **Confirmado: el export final a gestoría no depende de lo guardado en la base de datos.** `lib/exportar.cjs` reconstruye el `.xlsx` final descargando el excel original tal cual se subió (con todas sus columnas) y solo escribe la nota encima — así que ninguna columna se pierde nunca en la entrega final, pase lo que pase con la config de import. Lo que sí falta es mostrar en pantalla lo que ya se guarda (`concepto`, `importe`, `nota_final` — ya en la tabla `movimientos`, sin necesidad de guardar nada nuevo): es el mismo hueco #2 de arriba ("falta información en cada línea"), no un problema de almacenamiento.
- **Limitación de entorno (no es un bug): subir un excel no funciona en `localhost`.** Vercel Blob usa autenticación OIDC que solo está activada para los entornos Production/Preview, no para "development" — cualquier intento de subir un excel en local falla con `Vercel Blob: OIDC is enabled for this project, but not for the "development" environment`. Para verificar cambios que dependan de subir un excel, hay que probarlos en producción o insertar datos de prueba directamente en la base de datos (como se hizo para verificar el punto de arriba).
- ✅ **Arreglado (2026-08-02) — Candidatos de match ambiguo/combo no se guardaban en BD.** Cuando el matching automático (subida de excel del banco, "Recalcular facturas sin resolver") encontraba varias líneas con el mismo importe (`ambiguo`) o una combinación de 2 facturas (`combo_sugerido`), solo se guardaba el texto del motivo (`motivo_detalle`) — la lista real de candidatos vivía solo en memoria y se perdía al terminar la llamada. La pantalla de Facturas solo pintaba los botones para elegir candidato si se acababa de recalcular esa fila concreta a mano ("Buscar"), aunque el cruce ya estuviera hecho — mismo problema de estado efímero vs. persistido que [LarpManager](#) tuvo antes. Reportado por la usuaria como "he subido un banco y hay muchas facturas que se pueden matchear que no están matcheadas". Arreglado guardando los candidatos en una columna nueva `facturas.motivo_candidatos` (JSONB, migración perezosa igual que `motivo_tipo`/`motivo_detalle`) y haciendo que `FacturasTrimestre.js` los lea de ahí cuando no hay un resultado más reciente en la sesión. Verificado insertando una factura de prueba con importe ambiguo en la base de datos de prueba: tras `reintentarPendientes`, los candidatos quedan en BD y la fila los pinta correctamente sin tocar "Buscar", en una carga de página nueva.

## Backlog de pruebas y pulido (2026-07-28, pendiente de revisar)

La usuaria va a hacer varias pruebas con datos reales; según lo que salga, hay que volver sobre esto:

- [ ] **Check match** — revisar que el emparejamiento automático de facturas funciona bien con datos reales.
- [ ] **Afinar proceso de cierre de trimestre.**
- [ ] **Documentación a gestoría** — revisar qué se entrega y cómo.
- [ ] **Módulo de colaboradores** — afinar.
- [ ] **Archivos con dos facturas dentro del mismo PDF** (2026-08-02) — el parseo actual (`lib/facturas.cjs`) asume una factura por archivo; proveedores tipo DoYouSpain/Iberia a veces mandan un único PDF con dos. **El estado "factura futura" (el otro problema de estos proveedores: no emiten factura hasta que el servicio ha pasado) ya está construido, ver "Funcionalidades construidas".**
- [x] **Match entre pago y LarpManager.** Construido y probado con datos reales 2026-08-01/02 — varias rondas de fallos reales encontrados y arreglados: persistencia del botón de confirmar (antes se perdía al recargar), diéresis alemanas, apellido en vez de nombre completo, pagos "manual" sin Wire, y confirmado que un export de LarpManager con rango de fechas corto simplemente no trae ciertos pagos (no es un bug). El caso "varios candidatos con el mismo importe" se sigue sin verificar con una colisión real todavía.
- ✅ **Arreglado (2026-08-02) — Solucionar PayPal.** Probado con un export real (`Download (3).xlsx`, cuenta business de PayPal): la config antigua por letra fija (`textCols`, `importeCol: "I"`, `fechaCol: "B"`) apuntaba a las columnas equivocadas (fecha en B era en realidad "Hora"; importe en I era "Tarifa", no "Neto") — mismo tipo de problema que tuvo BBVA. Migrado a `"modo": "nombres"` como bbva/openbank: fecha = columna "Fecha", importe = columna "Neto" (el que de verdad afecta al saldo, no el bruto), texto = "Nombre" + "Tipo" + "Asunto" + "Nota" (esta última a veces tiene anotaciones manuales reales, ej. "refund Inconscience"). Verificado simulando el parseo contra el excel real: fecha/importe/texto correctos en las 5 filas, notaCol cae en la columna 42, confirmada vacía.
  - **Bug adicional encontrado con la misma prueba, ya arreglado:** un export "suelto" de un solo banco conserva el nombre de hoja que le puso el banco (ej. "Download (3)" en este PayPal real), no el nombre canónico ("paypal"). Al cerrar el trimestre, si esa era la única hoja subida, `generarExcelFinal` usaba el workbook original sin renombrar la hoja — el paso que escribe las notas la busca por nombre y no la encontraba, así que esa hoja se entregaba a gestoría **sin ninguna nota, en silencio**. Arreglado en `lib/exportar.cjs`: si solo hay una hoja subida y su nombre no coincide, se renombra antes de generar el excel final. No afecta al export combinado (varias pestañas en un mismo archivo), donde cada pestaña ya se llama como debe.

## Arquitectura de información — 3 pestañas por momento de uso (acordado 2026-07-27)

El problema de fondo detectado en la auditoría completa (más abajo) no era una lista de bugs sueltos: era que **una sola pantalla mezclaba todos los momentos de uso**, sin importar por qué se abría la app. Se decidió dividir la vista de un trimestre en 3 pestañas, cada una pensada para un momento concreto (mockup en Artifact, verificado con la usuaria antes de escribir código):

- **Inicio** — el momento de "voy a subir una foto rápida y ya". Lo primero y más grande que se ve es "Subir factura suelta". Debajo, un resumen mínimo (X de Y resueltas) con un enlace a "ver pendientes". Nada de excel, nada de checklist, nada de colaboradores.
- **Trimestre** (nombrada así, no "Proveedores") — la sesión de cierre: subida en lote destacada arriba, buscador + toggle "solo pendientes", el excel colapsado en un desplegable (no se toca cada vez que se entra), el checklist agrupado, y "Cerrar trimestre" al final — es parte de esta sesión, no una acción de un vistazo.
- **Colaboradores** — su propio espacio, ya no comparte scroll ni interrumpe las otras dos.

Esto sustituye y concreta el ítem suelto que había en la Fase 2 de la auditoría anterior ("separar en pestañas"). Línea visual de la reorganización: pendiente ("ya veríamos, vamos poco a poco" — la estructura se acordó antes que el acabado visual final).

## Auditoría de UX completa (2026-07-27)

Revisión de las 20 pantallas/componentes reales contra el flujo de arriba. Los ítems de arquitectura (🔴 1-3) ya se resolvieron con la reorganización en pestañas; el resto sigue pendiente de construir.

**Antes de cerrar / Trimestre** (`page.js`, `GrupoProveedor.js`, `SubirFactura.js`)
- 🔴 Sin subida en lote — ver "Huecos identificados".
- 🔴 Sin buscador/filtro de proveedores — con 290 grupos, encontrar uno es scroll a ciegas.
- 🔴 Sin vista "solo lo pendiente" — resueltos y no resueltos se listan mezclados.
- 🟠 Nada distingue visualmente los grupos "un clic y resuelto" (fija) de los que dan trabajo, salvo texto pequeño.
- 🟠 El estado "pedida, pendiente" no tiene vista propia — no hay un sitio único para ver todo lo que se está esperando de proveedores.
- 🟠 Confirmar un grupo entero no tiene deshacer.
- 🟡 El selector de excel no confirma qué detectó hasta después de subirlo.

**Colaboradores y lotes** (`SeccionLotes.js`, `SubirFacturaLote.js`, `lotes/[id]/page.js`, `colaborador/page.js`)
- 🔴 `SubirFacturaLote` tampoco admite varios archivos — y aquí pesa más, porque es la propia colaboradora quien repite la subida una a una por cada recibo de un evento.
- 🟠 La contraseña de colaborador se enseña una vez sin botón de copiar, y no se puede regenerar sin borrar el colaborador entero.
- 🟠 "Ver archivo" de una factura abre en pestaña aparte — se revisa importe/concepto sin ver la imagen a la vez.
- 🟠 Vincular un pago a una línea del banco es un `<select>` con todos los movimientos del trimestre sin filtrar.

**Transversal**
- 🟠 Un único formulario de login sirve para admin y colaboradora (usuario en blanco = admin) — fácil de equivocarse.
- 🟡 "Volver" desde un lote siempre lleva a "/", sin recordar desde qué trimestre se entró.
- 🟡 Formato de número inconsistente entre pantallas.

## Plan de UX (auditoría, 3 fases)

- **Fase 1 — confianza** ✅ hecho (commit `576d64c`, 2026-07-27): toasts de guardado/error, diálogos propios (`ConfirmDialog`, `MotivoDialog`) en vez de `window.confirm`/`window.prompt`, manejo de errores consistente en las llamadas a la API (`apiFetch`).
- **Fase 2 — en curso**: arquitectura en 3 pestañas ✅ acordada (ver arriba); dentro de "Trimestre" siguen pendientes el buscador/filtro, el toggle "solo pendientes" y la subida en lote.
- **Fase 3 — pendiente**: botones de guardar explícitos en móvil (el guardado solo por `onBlur` falla con el teclado virtual), botón de copiar la contraseña generada de un colaborador (+ forma de regenerarla), estados vacíos con guía, accesibilidad (el estado no debe depender solo del color), formato consistente de números/fechas, "ver archivo" de factura junto a los campos en vez de en pestaña aparte, buscador en el `<select>` de vincular pago.

## Línea visual (decidida 2026-07-27, aplicada al código el mismo día)

- Fondo blanco puro / neutro, sin ningún tinte de color en tarjetas ni bordes.
- ~~Acento único: azul de la identidad de NOT ONLY LARP, `#1d5da6`~~ — **sustituido el 2026-08-13 por índigo (`#4e4f74`), decisión consciente de romper la alineación con el color de marca real de la asociación (ver Registro de decisiones).** Texto y gris neutro también alineados con la marca: texto `#231f20`, gris `#58595b` (estimados a ojo del logo en PDF, no hay hex exactos documentados — corregir si aparecen). Decisión 2026-07-27: mantener Calibri (no la Montserrat Extrabold del logo), pero sí adoptar la paleta de color de la asociación.
- Categorías de proveedor (fija/factura propia/mixta/nueva) como texto simple junto al nombre (`.categoria-texto`), no insignias de colores — el "semáforo" de colores por categoría es justo lo que se rechazó.
- Los `.etiqueta.fija/.nueva/.pedida` que quedan (estado real aceptada/rechazada/pendiente en lotes y colaboradores) se mantienen coloreados a propósito — es estado semántico, no decoración — pero con tonos apagados (`--ok`/`--new` desaturados) en vez de verde/rojo saturados. `.etiqueta.mixta`/`--warn` se eliminaron el 2026-08-13 por ser CSS muerto (no referenciado en ningún componente).
- Tipografía: familia Calibri (`Calibri, Candara, "Segoe UI", system-ui, sans-serif`).
- Tema oscuro descartado explícitamente (ya se había descartado antes, en la sesión anterior, por el mismo motivo de fatiga visual en sesiones largas) — no hay `prefers-color-scheme` ni variante oscura, un único tema.
- **Aplicado** en `app/globals.css` (tokens, tipografía, `.etiqueta`, toasts, diálogos). Verificado en `localhost:3000` contra un trimestre de prueba real: fondo blanco, botón `rgb(44,67,88)`, fuente Calibri confirmados por JS en el navegador.
- **Pendiente**: no se ha vuelto a subir el tamaño de letra al nivel de los mockups v3 (16.5px nombres / 16px botones-inputs) — los botones/inputs de la app ya estaban en 16px de base, pero no se revisó el resto letra por letra contra el mockup. Revisar si hace falta subir más.

## Tabla plana de la pestaña Trimestre (2026-07-27, reemplaza el acordeón por proveedor)

`GrupoProveedor.js` (acordeón: había que abrir cada tarjeta de proveedor para ver algo) se sustituyó por `app/components/TablaMovimientos.js`, una tabla continua real — motivado directamente porque con ~50-300 grupos, la mayoría de 1 sola línea, "un clic por grupo" era en la práctica "un clic por línea" (~300 clics para ver todo).

**Estructura (versión final, implementada 2026-07-27 sobre `propuesta_completa.html`):**
- Sin acordeón: todos los movimientos visibles de entrada. Grupos de proveedor **solo se muestran con cabecera gris cuando tienen más de 1 movimiento** — un grupo de una sola línea es simplemente una fila más, sin cabecera (ya se había acordado, se coló una vez en un mockup intermedio y se corrigió). ~~La cabecera muestra nombre, categoría, X de Y resueltas y el **importe total real del grupo completo**, no del filtrado por "solo pendientes".~~ — **corregido 2026-08-15:** el total del grupo **se quitó** (sumaba también las líneas que "Solo pendientes" oculta, así que enseñaba un número que no cuadraba con nada de lo que había debajo), y el "X de Y resueltas" también. La cabecera lleva el texto del banco y su categoría en Concepto, y **el proveedor en la columna Proveedor** — antes el nombre del proveedor se pintaba en Concepto con el campo Proveedor vacío al lado, la misma cosa en dos sitios y en ninguno el suyo.
- **Columnas base, en este orden**: Fecha, Concepto, Banco, Proveedor, Importe, Estado, Factura, Nota, Proyecto. ~~7 columnas, sin columna Factura separada (decisión 2026-07-27: Factura pasa a ser parte de Estado)~~ — **decisión revertida (2026-08-02):** la usuaria la quiere como columna propia, no metida dentro de Estado. **Actualizado 2026-08-15:** la columna Factura enseña **el número** de cada factura, no la palabra "ver" — es como se llama su archivo dentro del zip que va a la gestoría, así que lo que se ve en pantalla y lo que ella recibe se llaman igual; con varias, separadas por coma y cada una abre la suya. Y se quitó la columna **Grupo** (los iconos "−" y "+"): ver Registro de decisiones.
- **Fecha y Concepto quedan fijas (`position: sticky`)** al hacer scroll horizontal — es la pestaña de escritorio ancho (`.contenedor-ancho`, 1400px en vez de 720px, solo aplicado a la pestaña Trimestre) y con columnas extra activas el scroll horizontal es habitual; sin esto se pierde de vista a qué línea corresponde cada celda.
- **Estado — desplegable siempre editable**, incluso cuando ya está resuelta: se puede revertir a mano una línea resuelta por error. Hoy son **seis** opciones, no tres: pendiente / pedida / factura futura / ignorar / devolución / resuelta (las tres últimas se añadieron el 2026-08-02, ver más arriba). El enlace "ver factura" ya no vive aquí (ver columna propia arriba).
- **Columna LarpManager** (extra, pero activada por defecto): lo que dio de sí el cruce contra el CSV. Cuatro mensajes posibles — el nombre y el evento de quien pagó, "N coincidencias posibles", "el importe no cuadra" y "no encontrada". Cuando la línea tiene su pago, un **✎** al lado quita el vínculo (ver Registro de decisiones, 2026-08-16). Vincular **no** se hace aquí: se hace desde la pestaña **LarpManager**, que es donde se ve el problema.
- **Columnas extra ocultables**: botón "Columnas" abre un panel con casillas — la lista sale dinámicamente de las claves presentes en `datos_originales` de los movimientos cargados. Ocultas por defecto, se pueden activar (ej. "Saldo") y aparecen como columna más al final de la tabla.
- **Buscador general**: filtra por texto en cualquier campo visible, incluidas las columnas extra activas.
- **Ordenar por columna**: clic en cualquier cabecera ordena por ese campo (asc → desc → vuelve a agrupado). Al ordenar se pierde el agrupado visual.
- **Nota**: un único `<input>`, guarda con Enter **y al salir del campo**, sin botón visible. ~~Se prellena en azul con la sugerencia aprendida cuando existe.~~ — **corregido 2026-08-15:** la sugerencia **nunca** se mete dentro del campo. Va aparte, como píldora, y o está la píldora o está el campo. El campo enseña **la nota que ya está guardada**, para poder corregirla o borrarla; y se puede editar aunque la línea esté resuelta.
- **Qué es la Nota** (aclarado 2026-08-16, porque estaba haciendo el trabajo de tres columnas): es **el texto que se escribe en el excel que se manda a la gestoría**, en la fila de esa línea. No es el sitio del proveedor (tiene su columna) ni el de la factura (tiene la suya). De 79 notas reales, 20 eran nombres de proveedor y 15 referencias de factura; solo 28 eran notas. **La app ya no escribe el número de factura en la nota**: al emparejar, la nota es el concepto que se escribió al subir la factura, y si no hay, se queda vacía.
- **Acciones de grupo — dos mecanismos separados, explícitamente no fusionados** (decisión 2026-07-27, tras dos rondas de corrección: primero se propuso un sistema de checkboxes/multi-selección que la usuaria rechazó porque no era el patrón ya usado en el resto de la tabla; después se fusionó "aceptar sugerencia" con "aplicar algo distinto" en un único control, y también se rechazó — "no tiene sentido que solo me dejes hacer la acción que sugieres tú"):
  1. Si hay una sugerencia aprendida para el grupo, una píldora de un clic con el texto aprendido y cuántas líneas cerraría (`ticket · 5 líneas`).
  2. Aparte, siempre disponible, el mismo patrón Nota+Estado que ya existe por línea pero a escala de grupo: un campo de nota + un desplegable de estado (pedida/resuelta), para cuando lo que hace falta no es la sugerencia sino algo distinto o cambiar el estado de todo el grupo de golpe.
  La **sugerencia** solo se muestra si al grupo le quedan líneas sin resolver. El **campo de nota**, en cambio, está siempre (corregido 2026-08-16): antes desaparecía en cuanto el grupo estaba entero resuelto, y entonces no había forma de corregir ni borrar su nota. Enseña la nota que comparten todas sus líneas, o vacío si cada una tiene la suya, que es lo que hace falta para poder borrarla.
- **Proyecto**: igual que ya estaba (desplegable + sugerencia de un clic).

**Base de datos**: `movimientos.datos_originales JSONB` guarda todas las columnas del excel original tal cual (migración `db/migration_datos_originales.sql`, ya aplicada). `lib/importarExcel.cjs` lee la fila de cabecera para los nombres de columna de cada banco.

**Endpoint** `POST /api/movimientos/:id/estado` (`{ estado: 'pendiente'|'pedida' }`) — cambia el estado individual de una línea vía `lib/agrupador.cjs#marcarLineaEstado` (antes `marcarLineaPendiente`; renombrado porque ahora es un desplegable de 3 estados, no un booleano, y a propósito no exige que la línea no estuviera ya resuelta, para poder revertir).

**Verificación final (2026-07-27)**: datos de prueba reales insertados directamente en la base de datos (`test-final`, limpiado después), probado en el navegador vía DOM/fetch (la herramienta de navegador visual fallaba con "0x0 viewport" esa sesión). Confirmado: cabecera de 7 columnas sin Factura aparte; grupo de 1 línea sin cabecera; grupo de 3 líneas con cabecera y total correcto (186.82€ sumando la línea ya resuelta, no solo las visibles con "solo pendientes" activo); columnas Fecha/Concepto con `position: sticky` y `left` correctos; panel "Columnas" activando "Saldo" en la tabla real; Estado revertido de resuelta → pendiente, persistido en base de datos.

## Registro de decisiones

Cada decisión de producto/alcance/diseño se anota aquí en cuanto se toma, con fecha.

- **2026-07-27** — Se reconstruyó este documento desde cero leyendo la sesión anterior completa (1598 mensajes), porque no existía como archivo — vivía solo dentro del modo Plan de esa conversación y se perdió el hilo entre sesiones. A partir de ahora se mantiene aquí para que no vuelva a pasar.
- **2026-07-27** — Aclarado que nada de la app está realmente testado en uso real: la "verificación con datos reales" fue solo a nivel técnico/API. El desastre se descubrió al intentar usarla de verdad, no antes. No dar por hecho que ninguna pantalla/flujo funciona bien en la práctica solo porque el código compile o la API responda.
- **2026-07-27** — Aplicada la línea visual acordada al código real (`globals.css` + `GrupoProveedor.js`), verificado en el navegador contra datos reales de prueba. Los badges de estado real (aceptada/rechazada/pendiente en lotes/colaboradores) se mantuvieron coloreados pero apagados — solo se quitó el color de las categorías de proveedor, que era decorativo, no de estado.
- **2026-07-27** — Corregido un error de concepto propio: la funcionalidad fundamental que faltaba no era "leer una carpeta de Drive desde el CLI", era la subida en lote de varias facturas a la vez desde el propio webapp (ver "Huecos identificados"). Se decide no tocar el CLI hasta que esto esté construido, porque hoy es lo único que cubre esa necesidad.
- **2026-07-27** — Acordado el flujo de facturas que llegan tarde: atajo opcional "es una de las pendientes" al subir, sin forzarlo como paso obligatorio (2-10 casos/trimestre vs. 200-500 líneas totales).
- **2026-07-27** — Auditoría de UX completa de las 20 pantallas/componentes, hecha contra el flujo real (no una checklist genérica). El hallazgo principal no era una lista de bugs sueltos: una sola pantalla mezclaba los tres momentos de uso sin distinguirlos.
- **2026-07-27** — Acordada la reorganización en 3 pestañas dentro del trimestre: Inicio / Trimestre / Colaboradores (ver "Arquitectura de información"). La pestaña de proveedores se llama "Trimestre", no "Proveedores". Estructura acordada primero; el acabado visual de esta reorganización queda para después ("vamos poco a poco").
- **2026-07-27** — Adoptada la paleta de color real de NOT ONLY LARP (azul `#1d5da6`, texto `#231f20`, gris `#58595b`) en vez del azul inventado en los mockups, a partir del logo/brand kit en PDF que pasó la usuaria. Se mantiene Calibri, no Montserrat. Colores estimados a ojo (sin hex documentado), verificado en el navegador contra el trimestre real.
- **2026-07-27** — Corregido el formato de columnas de la tabla de movimientos: de dos líneas por fila a una tabla real (`<table>`) con Fecha/Concepto/Proveedor/Importe/Nota — la usuaria lo pidió explícitamente como "tabla", no como tarjetas con texto corrido.
- **2026-07-27** — Feedback de proceso de la usuaria, importante para todo el proyecto en adelante: **el flujo de trabajo es idea → validar en palabras → implementar, en ese orden.** No escribir código a partir de una idea a medio formular sin confirmarla antes explícitamente, aunque parezca clara. Motivo: la usuaria corrigió dos veces seguidas en esta sesión ("y proyecto", "y proveedor") mientras ya se estaba implementando código sin haber confirmado el alcance completo primero.
- **2026-07-27** — Añadida la columna/funcionalidad "Proyecto": lista fija (no ligada a trimestre ni, de momento, a año — eso se aparca para cuando haga falta poner estados activo/cerrado), gestionada en su propio apartado `/proyectos` enlazado desde el menú general — explícitamente **no** dentro de la pestaña Trimestre (se preguntó y se corrigió esa colocación antes de terminar). Inferencia por coincidencia de texto contra los proyectos ya creados, nunca auto-asigna sin confirmar.
- **2026-07-27** — Regla de trabajo elevada de una entrada del registro a sección propia al principio del documento ("Regla de trabajo (fija, no negociable)"), porque se volvió a incumplir (se editó un mockup con `sed` justo después de que la usuaria pidiera "muéstramelo"). Aclarado explícitamente: "mostrar" = describir sin tocar archivos; "implementar" = editar y publicar. Son pasos separados, incluso para mockups desechables.
- **2026-07-27** — El campo `movimientos.concepto` ya guardado (usado para agrupar/matchear) es en realidad varias columnas del excel original ya fusionadas en un solo texto (`config/sheets.json`) — no había forma de ver esas columnas por separado en pantalla. Se decide guardar la fila completa aparte, sin tocar cómo se calcula `concepto` (ver sección "Tabla plana" arriba) — la usuaria confirma que los formatos de columnas pueden cambiar por banco en el futuro, pero decide no diseñar para eso todavía ("ya cruzaremos ese puente").
- **2026-07-27** — Rediseñada la pestaña Trimestre entera: de acordeón por proveedor (un clic por grupo para ver nada) a tabla plana continua, con columnas mostrables/ocultables, buscador general y orden por columna. Ver sección "Tabla plana de la pestaña Trimestre" arriba para el detalle completo — fue la sesión de iteración más larga del día, con varias rondas de "quita botones" hasta llegar a: Nota sin botón (Enter guarda), Estado como desplegable (no icono de acción), Sugerencia de grupo como texto (no botón), Sugerencia por línea prellenando el campo (no botón de aceptar separado).
- **2026-07-27** — Ronda final de diseño de la tabla (sobre `propuesta_completa.html`, mockup confirmado antes de tocar código real): (1) se quitó el verde de "resuelta" sin validar — nadie lo había aprobado para esta tabla — sustituido por gris neutro (sin factura) o el azul de acento ya usado en enlaces (con factura), sin introducir color nuevo; (2) Factura deja de ser columna propia y pasa a ser parte de Estado: un enlace "ver factura" bajo el desplegable cuando lo hay; (3) Estado pasa a ser un desplegable de 3 opciones siempre editable, incluida la reversión manual de una línea ya resuelta; (4) las acciones de grupo se mantienen como **dos mecanismos separados** (chip de sugerencia de un clic + Nota/Estado de grupo libres) tras dos rechazos explícitos de alternativas (checkboxes/multi-selección, y fusionar ambos en un único control) — la usuaria necesita poder actuar distinto a la sugerencia, no solo aceptarla o nada; (5) Fecha/Concepto fijas con scroll horizontal y contenedor ancho (1400px) solo en esta pestaña, por ser de uso de escritorio. Implementado en `TablaMovimientos.js`, `lib/agrupador.cjs`, `app/api/movimientos/[id]/estado/route.js` y `globals.css`; verificado en el navegador contra datos de prueba reales (ver detalle en la sección de arriba) antes de limpiar el trimestre de prueba.
- **2026-08-13** — Sustituido el acento azul de marca (`#1d5da6`) por índigo (`#4e4f74`) en toda la app, y aplicado "sin cajas" a los controles dentro de tablas (Movimientos/Facturas/Cuentas: `select`, `.secundario`, `.link-factura`, `.btn-quitar`/`.btn-unir`/`.btn-editar-mini` pierden su caja permanente en reposo, aparece igual para todos solo al tocar/enfocar). Ambos cambios surgen de una exploración de maquetas fuera del código real (`maqueta_final.html`, `maqueta_simplificar_acciones.html`) narrowed a 4 finalistas de color y comparados con distintos grados de simplificación de controles. **Decisión consciente y explícita**, confirmada tras pregunta directa: el azul anterior no era una elección estética libre, era el color de marca real de NOT ONLY LARP tomado de su logo (ver entrada 2026-07-27 "Adoptada la paleta de color real..."); se acepta romper esa alineación. Alcance de "sin cajas" limitado a controles dentro de `.celda` — botones/enlaces fuera de tabla (Login, diálogos, CTA de Inicio) no se tocan, no se han maquetado así. Eliminados `--warn` y `.etiqueta.mixta` por ser CSS muerto (confirmado vía grep, ninguna clase `.etiqueta.*` se renderiza en JSX salvo esta comprobación puntual); `--ok`/`--new` se mantienen sin cambios (toasts de éxito/error, texto de error en Login/Invitación/Restablecer) — son semánticos, no parte del acento.
- **2026-08-13** — Detectada y corregida una inconsistencia real señalada por la usuaria al entrar en Proyectos: convivían **3 modelos de tabla distintos** en la app. (1) Movimientos/Facturas: tabla CSS Grid desnuda, sin envoltorio. (2) Colaboradores/Cuentas/Proyecto (detalle): la misma tabla pero metida dentro de `.tarjeta` (borde+sombra+padding de card). (3) Proyectos (lista): ni siquiera una tabla real, `<div className="fila">` sueltos sin cabecera ni columnas. Confirmado el modelo de Movimientos como el único correcto — se quitó `.tarjeta` de `TablaColaboradores.js`, `TablaCuentas.js` y `app/proyectos/[id]/page.js` (las 3 tablas), y la lista de Proyectos en `GestionProyectos.js` se convirtió al mismo patrón de rejilla con cabecera "Nombre". De paso se encontró y arregló `app/proyectos/page.js`: una ruta huérfana (sin ningún enlace real hacia ella en la app, confirmado por grep) que no usaba `<CabeceraApp>` — tenía un `<h1>`+"Volver" sueltos, sin menú. Se le añadió `<CabeceraApp>` igual que el resto de páginas independientes (`/lotes/[id]`, `/proyectos/[id]`). Añadida regla permanente arriba ("La tabla nunca va envuelta en `.tarjeta`") para que no se repita. Verificado en el navegador contra datos reales: las 3 tablas y la lista sin `.tarjeta` (confirmado por `getComputedStyle`), menú completo en las 3 rutas de Proyectos.
- **2026-08-13** — Tras el cambio anterior, la usuaria insistió en que la página de un proyecto concreto seguía sin estar bien — dos bugs reales que la comprobación anterior no había pillado, encontrados por inspección directa de `getComputedStyle` en el navegador (no asumidos): (1) `<h1>{proyecto.nombre}</h1>` no tenía ninguna clase, así que heredaba el tamaño por defecto del navegador (32px) muy por encima de toda la escala tipográfica de la app (12.5-18px) — mismo bug en `/lotes/[id]`. Corregido con una clase nueva `.titulo-pagina` (18px, mismo peso que la marca "Punteo"). (2) Bug más grave: en las páginas sueltas (`/lotes/[id]`, `/proyectos/[id]`, `/proyectos`) las pestañas del menú se renderizan como `<a>` (vuelven a `/?tab=x`), pero el CSS de `.tabs-cabecera` solo apuntaba a `button` — nunca se cubrió el caso `<a>`. Resultado: en esas páginas el menú llevaba **cero estilos propios**, enlace azul subrayado por defecto del navegador en vez del menú real. Mismo bug en el menú móvil (`.menu-movil-lista` solo apuntaba a `button`). Corregido añadiendo `a` a todos los selectores de `.tabs-cabecera` y `.menu-movil-lista`. De paso se implementó el menú "subrayado depurado" elegido por la usuaria entre 4 opciones maquetadas (`maqueta_menu.html`, opción A) — marca con más peso (18px, 700), raya activa más gruesa y redondeada, más aire en el padding. Verificado con `getComputedStyle` en escritorio y en el menú móvil (viewport 600px) contra `/proyectos/4` real.
- **2026-08-13** — Añadida la pestaña **Ayuda** al menú principal: manual de uso con interruptor Administración/Colaboradores (`app/components/Ayuda.js`), escrito a partir del código real. Pendiente de completar con tres procesos manuales que la usuaria va a detallar (de dónde sale el excel del banco, qué hace antes/después de "Enviar a gestoría", criterio real de cierre de proyecto) — no están en el código, no se pueden inventar.
- **2026-08-13** — Corregido un hueco real en "facturas de NOL": antes una factura general de NOL no llevaba proyecto ni distinguía quién la pagó, era indistinguible de cualquier otra factura sin emparejar. Ahora quien tiene el permiso `puede_subir_facturas_generales` elige, una vez por tanda de archivos, **quién paga** (NOL o el propio colaborador) y el **proyecto**; cada archivo de la tanda lleva su propia fecha/concepto/importe (`app/components/SubirFacturasNol.js`). Si paga el colaborador, entra por su lote de ese proyecto (se crea el lote al vuelo si no existía, `lib/lotes.cjs#buscarOCrearLote`); si paga NOL, sigue el motor de facturas generales pero con `facturas.proyecto_id` puesto (columna nueva, migración `db/migration_facturas_proyecto_id.sql`, aplicada en dev y producción). "Facturas de colaboradores pendientes" del proyecto ahora incluye también las de NOL, con columna "Paga" para distinguirlas. Verificado contra la base de datos de dev con datos reales (find-or-create de lote idempotente, consulta UNION correcta) y limpiado después.
- **2026-08-13** — Colaboradores traducido por completo al inglés (`/colaborador`, subir factura, invitar, login/recuperar/restablecer también a petición explícita aunque son pantallas compartidas con admin). `TablaCuentas.js` (compartida) cambia de idioma según el prop `soloLectura`, que solo es `true` en la vista de colaborador.
- **2026-08-13** — Unificada la subida de facturas de colaborador en un único formulario tras feedback real de la usuaria probándolo ("es un poco cristo"): sustituye las dos cajas separadas (tu lote / facturas de NOL) por una sola, con "Quién paga" visible solo si tienes el permiso NOL (marcado "Yo" por defecto).
- **2026-08-13** — Simplificación mayor pedida por la usuaria acto seguido ("yo doy de alta, ellos eligen a qué proyecto suben facturas y ya"): el alta de colaborador **ya no asigna proyecto**. Cualquier colaborador (no solo quien tiene el permiso NOL) elige el proyecto cada vez que sube una factura; se le crea el lote al vuelo la primera vez (`buscarOCrearLote`, ya construido para NOL). El permiso NOL se mantiene aparte: sin él, siempre "paga yo". `listarTodosLosLotes` pasa de `JOIN` a `LEFT JOIN` desde colaboradores para que quien todavía no ha subido nada siga apareciendo en la tabla de administración. Sin migración de datos: los lotes ya creados se reutilizan tal cual. Verificado con una colaboradora de prueba real sin lotes: visible en admin, sube factura eligiendo proyecto sin el permiso NOL, intento de "paga NOL" sin permiso bloqueado con 403.
- **2026-08-14** — Limpieza de la barra de acciones de Movimientos (opción "E" de una propuesta de rediseño de 5 direcciones; la usuaria eligió esta por ser la de menor riesgo). Se arreglaron nombres objetivamente rotos: había **dos botones distintos etiquetados "Excel"** (uno abre el diálogo de subir excel del banco, otro lista/borra los excels ya subidos) y dos con la jerga "LM"/"Match LM". Nuevos nombres tomados literalmente del título que ya tiene cada diálogo al abrirse, no inventados: "Excel del banco", "Pagos de LarpManager", "Pagos sin emparejar", "Excels subidos". "Devoluciones" y "Enviar a gestoría" ya coincidían y no se tocaron. Añadido un indicador de avance a la derecha de la fila (`.bloque-avance`): barra + "X de Y resueltos", contando solo lo que necesita factura de verdad — se excluyen ignoradas y facturas futuras, que no dependen de nadie ahora mismo. El "N pendientes" clicable de la barra de la tabla se mantiene intacto. Se probaron rótulos de texto encima de cada grupo de botones (SUBIR/REVISAR/ENVIAR) y **la usuaria los descartó** al verlos en la app real: el divisor vertical solo ya basta. Diagnóstico previo medido sobre el DOM real, no a ojo: 11 columnas (1.225px), 6,6 controles interactivos por fila, ~172 controles en pantalla con 26 pendientes — la densidad de la tabla sigue sin abordarse, es lo que quedó fuera de esta opción.
- **2026-08-14** — Método de trabajo acordado para cambios visuales, sustituye al de maquetas HTML aparte: **se implementa en la rama de git sobre el entorno de desarrollo y la usuaria lo mira en la app real** (`localhost:3000`), en vez de construir un mockup que reproduce la página. Motivo, en sus palabras: "no te estoy pidiendo más que no reproduzcas de memoria lo que estamos publicando en producción". Revertir es `git checkout main`; a producción solo llega con un "commit push" explícito. Si por lo que sea hace falta un mockup aparte, el contenido se extrae literal del DOM real ("extracción real"), nunca se retipea. Se retiró el hook `auditoria-obligatoria.cjs` que exigía un informe de auditoría antes de publicar maquetas: no evitaba el problema real (publicar sin autorización explícita), porque el informe lo generaba el propio Claude — pasar la comprobación nunca fue lo mismo que tener permiso.
- **2026-08-15** — **La sugerencia del sistema pasa a ser una sola forma, con forma de rechazarla.** Hasta hoy lo único que distinguía una propuesta de la app de un dato escrito a mano era que pusiera la palabra "Aplicar" — y en cuatro sitios (nota, proveedor, proveedor de grupo, jugador de una devolución) ni siquiera eso: la propuesta se escribía **dentro del campo**, en negro, indistinguible de lo suyo. Cómo se llegó ahí, auditado en git: el commit `88bfdfc` (2026-08-03) borró la clase `.prellenado`, que era la que marcaba esos campos, y dejó `.chip-sugerencia` como texto pelado; el `46c92f9` (2026-08-13) cambió el acento a índigo sin comprobar si las sugerencias seguían distinguiéndose. Ahora hay **una única píldora `.sugerencia`** para los siete tipos, con la ✕ dentro, y **desaparece la palabra "Aplicar"**: la forma ya dice que es una propuesta. Regla que vino después de verlo funcionando (`19a91ca`): **o la píldora o el campo, nunca las dos cosas** — mientras hay sugerencia se ve solo ella, y la ✕ deja el campo vacío como si nunca hubiera existido ("pon la sugerencia, si la quito ya escribiré yo"). Y **a una línea ya resuelta no se le propone nada**: su respuesta ya está dada.
- **2026-08-15** — **El orden de la tabla deja de ser por tipo de gasto y pasa a ser por dónde conviene empezar** (`811a841`, rescatado de una rama abandonada): primero lo que ya trae alguna sugerencia, porque se cierra de un clic y despeja la pantalla; dentro de eso, de más líneas sin resolver a menos, porque buscar una factura para un grupo de tres cierra tres líneas y no una. Lo completo, al final.
- **2026-08-15** — **Tres fallos de lectura de facturas, encontrados usándola** (`b750d70`): (1) un PDF con **varias facturas dentro** (Amazon manda una por vendedor) leía los dos totales y **se quedaba solo con el mayor**, tirando el resto en silencio — ahora se prueban también las sumas de pares y tríos, y **una suma nunca resuelve sola: se propone**; (2) las **fechas con puntos** (`19.07.2026`) y pegadas a su etiqueta (`Fecha del pedido19.07.2026`) no se detectaban, y un archivo entero se quedaba sin ninguna fecha — que es lo único que desempata cuando dos líneas tienen el mismo importe; (3) cuando el gasto **ya estaba cubierto** por otra factura, el aviso decía "no coincide con ninguna línea pendiente" — cierto, pero no decía lo único que hace falta saber: que no falta nada.
- **2026-08-15** — **Se retira la columna Grupo (los iconos "−" sacar del grupo y "+" unir a un grupo)** (`46aa597`). Agrupar y desagrupar se hace con el campo Proveedor, que hace lo mismo y mejor: cruza textos distintos del banco **y bancos distintos** — "amazon" junta líneas de bbva y de openbank —, cosa que el "+" no hacía. Además el "+" **nunca llegó a funcionar**: su panel se dibujaba dentro de una celda de 40px con recorte activo, así que quedó oculto desde el día que se añadió (2026-08-03) hasta el día que se quitó. El backend (`/separar`, `/unir`) se deja intacto para poder rescatarlo en un commit si algún día hace falta separar líneas que el banco escribe idénticas — el único caso que el Proveedor no cubre. En el mismo cambio: **quitar un proveedor ahora olvida lo aprendido**; antes se desagrupaba la línea pero el nombre seguía guardado, así que la app lo volvía a proponer acto seguido — le decías que no y no se enteraba.
- **2026-08-15** — **El campo de Importe se tragaba en silencio cualquier cantidad de mil para arriba escrita a la española** (`7b9e726`): cambiaba solo la primera coma por un punto, y "2.183,18" dejaba de ser un número. No guardaba, no buscaba y no avisaba de nada. La app ya sabía leer bien esos números al extraer PDFs; ahora las pantallas usan **el mismo lector** (`lib/numero.cjs`, sin dependencias de Node para poder usarse en las dos partes) y, si no entienden lo escrito, lo dicen. En el mismo cambio: **el aviso de las facturas que no encuentran línea sale en Facturas**, que es donde se suben y donde se arreglan, y no en Movimientos con un campo para repetir el importe que ya se había escrito ("no tiene sentido alguno que me aparezca en OTRA PUTA PANTALLA y no donde se produce el error"); y **las devoluciones a la tarjeta dejan de agruparse entre ellas** — el banco escribe siempre el mismo texto y solo cambian tarjeta y fecha, que se limpian a propósito, así que tres devoluciones de tres compras distintas caían en el mismo grupo.
- **2026-08-15** — **La nota de una línea resuelta se puede ver, corregir y borrar** (`557960f`): era texto plano, y una nota mal escrita se quedaba mal escrita para siempre salvo revirtiendo la línea a pendiente.
- **2026-08-15** — **La columna Factura enseña el número, no la palabra "ver"** (`5e2d121`). Con varias, separadas por coma y cada una abre la suya — antes "ver" abría siempre la primera. El número es el mismo con el que se llama el archivo dentro del zip de la gestoría, así que lo que se ve en pantalla y lo que ella entrega se llaman igual.
- **2026-08-15** — **Fuera el total de importe de la cabecera de grupo** (`3e6b4c9`): sumaba también las líneas que "Solo pendientes" oculta, así que enseñaba un número que no cuadraba con nada de lo que había debajo. También se quitó el "X de Y resueltas" ("no me digas cuántas resueltas y ponlo donde toca, en proveedor") y **el proveedor se movió a su columna** — antes se pintaba en Concepto con el campo Proveedor vacío al lado: la misma cosa en dos sitios y en ninguno el suyo.
- **2026-08-16** — **Fuera "Subir factura" en las líneas cerradas** (resueltas o ignoradas) (`ab478c7`): si una línea está resuelta y no tiene factura, es que no lleva. Las que sí esperan una — pendiente, pedida y factura futura — conservan su botón. En el mismo cambio: **la nota de un grupo se puede editar y borrar** (el campo desaparecía en cuanto el grupo estaba entero resuelto, y entonces no había forma de tocarla) y **editar una devolución vuelve a ser posible** — el ✎ activaba el modo de edición pero la pantalla comprobaba antes "es una devolución" y volvía a pintar el texto, así que el botón no hacía absolutamente nada.
- **2026-08-16** — **La Nota estaba haciendo el trabajo de tres columnas, y se separan** (`cf91b49`). Medido sobre los datos reales de producción, no a ojo: de 79 notas, **20 eran nombres de proveedor** (stripe, banco, dgm, impuestos… que en realidad explican *por qué no hay factura*), **15 eran referencias a un archivo de factura**, **16 estaban vacías** y solo **28 eran notas de verdad**. El motivo es histórico: la Nota es lo más viejo de la app y, cuando se hizo, no existían ni la columna Proveedor ni la de Factura, así que ahí fue a parar todo. Decidido: **la nota es el texto que se escribe en el excel de la gestoría y nada más**; la app deja de meter ahí el número de factura, en los cinco caminos que lo hacían. Al emparejar, la nota pasa a ser el concepto que se escribió al subir la factura, y si no hay, se queda vacía.
- **2026-08-16** — **El excel de la gestoría lleva todo lo que la app sabe**, no solo la nota (`cf91b49`): Nota, Proveedor, Proyecto, Facturas, Jugador y LarpManager, seis columnas escritas a partir de la primera columna libre del excel original. El Proveedor no le hace falta a la gestoría, pero va igual porque es la referencia con la que ella reconoce cada gasto.
- **2026-08-16** — **"Aquí no va nota" pasa a ser una respuesta aprendida** (`cf91b49`). Antes la app solo aprendía cuando se escribía algo, así que resolver veinte líneas del mismo tipo dejándolas en blanco no le enseñaba nada y las seguía tratando como sin clasificar. Ahora, cuando lo aprendido es el vacío, la app **no propone nada** en ese tipo de movimiento.
- **2026-08-16** — **Las facturas de colaborador llegan a la gestoría, con la misma numeración que las demás** (`cf91b49`). No llegaban: el zip las excluía y tenían numeración aparte (iban por 90001 mientras las propias iban por 24). Ahora hay **una sola numeración** (`siguienteNumero` mira la tabla entera, no cada flujo por su lado) y entran en el paquete por su vía real (factura → pago → línea del banco). La regla, decidida explícitamente, es **"las que se pagaron"**: basta con que su pago esté conciliado contra una línea del envío, sin mirar su estado de revisión — si una se rechaza después de haberla pagado, el dinero salió igual y el justificante tiene que ir. Los **huecos de numeración se aceptan a propósito**: una factura que se sube y luego se rechaza se lleva su número, y se prefiere eso a renumerar al enviar, que cambiaría un número que el colaborador ya ha visto.
- **2026-08-16** — **Rechazar una sugerencia se guarda para siempre** (`1a89c98`). La ✕ solo duraba hasta recargar —y volver de una subida recarga—, así que las sugerencias descartadas reaparecían una y otra vez: reportado como "Paypal la estoy aceptando a nivel de grupo y me sale una y otra vez". El rechazo **vale para todo el tipo de movimiento**, no solo para la línea donde se pulsó. En **nota y proveedor, rechazar es olvidar lo aprendido**; en proyecto, devolución y jugador se anota en una tabla nueva (`sugerencias_rechazadas`, migración `db/migration_sugerencias_rechazadas.sql`), guardando **qué** se rechazó para no bloquear de más — decir que no a "Glitz" no impide que mañana se proponga "Wield 2". La ✕ del proveedor es la excepción deliberada: **calla sus cuatro fuentes de golpe**, porque ahí la ✕ significa "aquí no hay proveedor" — si solo rechazara el nombre concreto, en una línea de banco o de impuestos habría que insistir tres o cuatro veces. **LarpManager se queda fuera** a propósito: sus propuestas son candidatos de un pago concreto, no una regla del tipo de movimiento, y siguen siendo de sesión. **Aviso: un rechazo no se puede deshacer desde la aplicación** — se planteó y la usuaria decidió no construirlo de momento.
- **2026-08-16** — **La importación de LarpManager, al mismo nivel que la del banco** (`bd8cf72`; el diagnóstico completo está en "Huecos identificados"). Guarda el CSV original, registra la subida, cada pago sabe de qué subida vino, y se puede **borrar una subida entera** — que se lleva sus pagos y **no toca las líneas del banco**: siguen resueltas y con su nota, solo pierden la comprobación de que ese ingreso llegó, y se recupera volviendo a subir el CSV. Guarda **todas** las filas con la fila entera del CSV, no solo las que se cruzan: de las 75 filas del CSV real se tiraban 43 sin dejar rastro. Decisiones concretas que se tomaron aquí:
  - **El filtro se mueve de la importación al cruce**, así que la regla se puede cambiar y volver a cruzar sin subir el archivo otra vez.
  - **La regla es una lista de exclusiones, no una lista cerrada de admitidos**: se cruzan las transferencias (`Wire`) y las filas **sin método de pago**, salvo `larpmoney` y `larpmanager`, que son apuntes internos y nunca llegan a la cuenta. Dicho por la usuaria: "ojo, se cruzan wire más vacías y que no ponga larpmoney y larpmanager — que van a salir más movimientos a futuro"; si mañana LarpManager escribe otra cosa en `Info` con el método vacío, se cruzará sola sin tocar nada. Del CSV real: 32 se cruzan, 43 se guardan sin cruzar (21 Stripe, 9 Redsys, 10 larpmoney, 3 larpmanager).
  - **Cómo se sabe que dos filas son la misma al volver a subir el archivo**: el CSV **no trae ningún identificador de pago** (13 columnas, ninguna es un id), así que dos filas son la misma cuando **todas** sus columnas coinciden. La posición en el archivo no sirve: LarpManager pone los pagos nuevos arriba y desplaza el resto. Si un CSV trajera dos filas idénticas de verdad, se guardan las dos.
  - Los pagos anteriores a este cambio se quedan como estaban, **sin subida asociada**: siguen funcionando, y borrar una subida nueva no los alcanza.
  - **"Excels subidos" pasa a llamarse "Archivos subidos"** y lista las dos clases, distinguiendo cuál es de banco y cuál de LarpManager.
- **2026-08-16** — **Borrar un excel subido reventaba** con un error de clave ajena (`pagos_movimiento_id_fkey`) si alguno de sus movimientos tenía un pago de colaborador conciliado. Encontrado por accidente al limpiar los datos de una prueba, no buscándolo. Ahora se **desvincula el pago primero**: sobrevive sin línea, listo para volver a conciliarse.
- **2026-08-16** — **Limpieza de datos de prueba en producción**: se borraron el proyecto "ZZ Prueba (borrar)", su colaborador, su lote, sus 3 facturas y su pago, comprobando antes y después que no tocaban nada real. **Los 3 archivos de esas facturas siguen en el almacenamiento**: el borrado de Vercel Blob falla desde el entorno local por no haber token (`BLOB_READ_WRITE_TOKEN` no está ni en `.env` ni en `.env.local`), y se decidió no perseguirlo.
- **2026-08-16** — **"Archivos subidos" daba error en producción** (`d647708`), desde el mismo día que se subió la importación de LarpManager. La pantalla lee dos columnas (`importaciones.origen`, `larpmanager_pagos.importacion_id`) que se crean solas la primera vez que la app toca algo de LarpManager, pero era **la única que las leía sin crearlas**: si era lo primero que se pulsaba, fallaba la consulta entera. Comprobado contra la base de datos de producción que ninguna de las dos existía, y reproducido el error exacto en desarrollo antes y después del arreglo. **Aprendizaje que se repite:** al añadir una migración perezosa hay que buscar **todos** los sitios que leen esas columnas, no solo el que las escribe. Volvió a pasar el mismo día en `construirProveedores`, y se evitó por haberlo tenido presente.
- **2026-08-16** — **La pestaña Ayuda, reescrita de cero** (`0c8fdbc`). La anterior describía una app que ya no existe: la sugerencia como "Aplicar: X" con una clase borrada, los iconos "−" y "+" de agrupar retirados, la columna "Ver factura", el Estado con tres opciones cuando son seis, la nota prellenándose dentro del campo, y los botones por nombres cambiados dos semanas antes ("Excel", "LarpManager"). Escrita **leyendo las pantallas una por una**: cada botón, columna, estado y motivo que se nombra está sacado literal del código. Es la corrección de un fallo anterior — el manual del 2026-08-15 se entregó nombrando elementos inexistentes por haberse escrito de memoria.
  - **Y ahora los colaboradores tienen la suya**, en inglés, que es el idioma real de su pantalla. Su pantalla no tiene barra de pestañas, así que se abre con un botón **Help** en la cabecera y se cierra con **Back**. Es el mismo texto que ve administración desde el interruptor, no una copia que se pueda desincronizar.
  - Quedan fuera a propósito, por no estar en el código: de dónde sale el excel del banco, qué se hace antes y después de "Enviar a gestoría", y el criterio real para cerrar un proyecto. Siguen pendientes de que la usuaria los detalle (identificado ya el 2026-08-13).
- **2026-08-16** — **Borrados los 32 pagos de LarpManager de producción** para volver a subir el CSV limpio. Decisión de la usuaria ("no dedicamos recursos a algo que corrijo subiendo un archivo nuevo") ante un problema real: esos pagos eran de antes del cambio de almacenamiento y **no tenían firma**, que es lo que ahora identifica una fila. Un valor vacío no es igual a nada en base de datos, ni a otro vacío, así que al volver a subir el CSV **se habrían insertado otra vez**: comprobado reproduciendo el estado exacto de producción en desarrollo, 32 pagos pasaban a 107, con 32 duplicados, "Pagos sin emparejar" de 6 a 61, y 15 líneas del banco reclamadas por dos pagos a la vez. Se descartó adoptarlos (reconocerlos por nombre+importe y rellenarles la firma) por no merecer el trabajo frente a volver a subir el archivo. Tras el borrado y la nueva subida: 75 filas, 32 cruzables, **26 emparejadas — las mismas que antes**, sin perder nada.
  - **Hallazgo de camino:** los 32 pagos viejos tenían **la fecha un día antes** de la que dice el CSV, un fallo del código anterior. El de ahora las guarda bien, comprobado con las 75 filas del archivo real. Es decir, las fechas de LarpManager en producción son correctas por primera vez.
- **2026-08-16** — **"Pagos sin emparejar" listaba 43 pagos que no pintaban nada ahí** (`b3ed5c0`), descubierto por la usuaria en la primera subida real: 49 filas de las que solo 6 eran de verdad. Las otras 43 eran las de pasarela (21 Stripe, 9 Redsys) y los apuntes internos (10 larpmoney, 3 larpmanager) — las que **por decisión explícita no se cruzan nunca**, porque ese dinero no llega a la cuenta como línea propia. Encima se les calculaba un motivo, así que la pantalla le decía que no encajaban con ninguna línea; claro que no encajan. Roto el día anterior al pasar a guardar todas las filas del CSV: el filtro se movió al cruce, pero esta consulta se quedó pidiendo "todos los que no tienen línea" sin mirar si alguna vez iban a tenerla. Antes no hacía falta la condición porque esas filas ni se guardaban. **Mismo patrón que el fallo de "Archivos subidos" del mismo día:** cambiar qué se guarda obliga a revisar todo lo que lo lee.
- **2026-08-16** — **Vincular a mano un pago de LarpManager con su línea del banco** (`b8c4215`). Hasta ahora, cuando el cruce acertaba se podía confirmar, y **cuando fallaba no se podía hacer nada**: la lista de "Pagos sin emparejar" era solo de lectura y el único botón de confirmar vivía en la píldora de la tabla, que solo aparece si el cruce ya ha encontrado al candidato él solo. Y hay casos que no va a acertar nunca: el banco no siempre escribe el nombre en el concepto — en producción hay una transferencia que pone literalmente "ABONO POR TRANSFERENCIA A SU FAVOR RECIBIDA EN EUROS LIQ. OP. Nº 000" y nada más. Sin nombre no hay nada que comparar.
  - **Vive en "Pagos sin emparejar"**, con el mismo patrón que Facturas ya usa cuando una factura no encuentra su línea. Se descartó una segunda vía desde la propia tabla de Movimientos (se construyeron las dos para poder compararlas en la app real): la usuaria eligió una sola, para no tener dos sitios que hacen lo mismo.
  - **El desplegable abre solo con las líneas del mismo importe.** Es lo único que de verdad desambigua: de 25 candidatas pasa a 1 en el caso real. Detrás de "Ver todos" están las demás, y si ninguna cuadra se enseñan todas directamente (un desplegable vacío parecería roto). El primer intento ofrecía las 25 de golpe y la usuaria lo rechazó al verlo: "encontrar el movimiento es imposible en cuanto haya muchos".
  - **Fuera las marcadas como Stripe** (9 de 25): son liquidaciones de la pasarela, un solo ingreso que agrupa muchas inscripciones, nunca la transferencia de una persona. Las de "ingresos ticket" **sí se quedan**, corregido por la usuaria sobre la marcha — justamente la línea sin nombre de arriba está marcada así.
  - **NO se filtra por estado, aunque lo parezca lo natural.** Se planteó ocultar lo resuelto y se descartó con los datos delante: lo normal es que la línea **ya esté resuelta**, porque las transferencias se puntean a mano antes de subir el CSV. De 25 candidatas, 17 lo están. Filtrar por estado dejaría 8 y quitaría justo las que hacen falta — es el mismo hallazgo del 2026-08-14, cuando de 12 pagos sin emparejar los 12 tenían su línea ya resuelta.
  - **Deshacerlo se hace con un ✎** en la columna LarpManager, solo en las líneas que tienen pago, con el mismo icono pequeño sin caja que editar una devolución. Devuelve el pago a la lista y quita el nombre de la columna, pero **no toca el estado de la línea**: no se guarda si estaba resuelta de antes o si la resolvió el propio vínculo, así que revertirlo sería adivinar. El diálogo lo dice y el desplegable de Estado siempre está a mano.
- **2026-08-16** — **Si el nombre coincide pero ningún importe cuadra, el cruce ya no propone nada** (`b8c4215`). Antes, si solo había un nombre coincidente, se pintaba como un **match normal** — con su píldora, y sin decir el importe — así que una sugerencia con la cantidad equivocada se veía **exactamente igual** que una perfecta. El caso realista es alguien que paga en dos veces (90 € de señal y 350 € después, que es literalmente el caso de Nick Ceccon ya documentado el 2026-08-14): el nombre coincide con las dos líneas y no había forma de ver cuál se estaba proponiendo. Decisión de la usuaria, y la razón importa: **un descuadre significa que el dato de LarpManager está mal, y eso se arregla en LarpManager**, no cerrando aquí la línea con el pago que no es. La columna pasa a decir "el importe no cuadra" (no "no encontrada", que sería mentira: el nombre sí se encontró) y el pago se queda en "Pagos sin emparejar", que ya lo explica con los números. **Consecuencia asumida a propósito:** una línea con el importe mal ya no se va a resolver sola nunca; la app deja de tapar el descuadre. Comprobado que no había ninguno mal de antes: los 26 enlaces existentes cuadran, y de las 15 sugerencias vivas ninguna era de este tipo.
- **2026-08-16 (noche)** — **Los lectores de archivos dejaban fuera el nombre de quien paga.** El excel del banco se reconocía bien, pero en el export real de BBVA **la columna del ordenante viene SIN RÓTULO** en la fila de cabecera, y la app buscaba las columnas de texto por su nombre — así que esa columna se caía en silencio. En el archivo real son **147 de 222 filas sin nombre**. Lo mismo con la columna de anotaciones a mano. Se invierte el criterio: cuando la config trae `textoExcluir`, entra **toda** columna de la fila salvo las que se excluyen a propósito (fecha, importe, saldo, divisa, oficina, código, fecha valor). Así una columna nueva o sin rótulo entra sola en vez de perderse. Acotado por la izquierda (a la izquierda de la fecha solo está el número de fila) y por la derecha (la cola de columnas vacías del excel). Aplicado solo a bbva. **Y el lector de LarpManager acepta ahora `.xlsx` además de `.csv`**, buscando la fila de cabecera en vez de fijarla: en un export está en la 1 y en otro en la 2.
  - **Consecuencia que no se previó y costó una limpieza:** cambiar el texto cambia el `concepto`, y la app reconoce una fila repetida por fecha+importe+**concepto**. Al volver a subir el mismo archivo después del cambio, las 222 filas parecieron nuevas y se duplicaron. **Regla para la próxima: tocar cómo se lee un archivo obliga a avisar antes de que se vuelva a subir.**
- **2026-08-16 (noche)** — **Un pago justifica una sola línea del banco, y una línea un solo pago.** No se garantizaba: el cruce iba línea por línea y, para cada una, buscaba entre todas las filas del archivo sin saber cuáles ya se habían usado — dos cuotas de 150 € de la misma persona encontraban las dos el mismo apunte. Ahora el reparto se hace **de una vez para todo**: se forman todas las parejas posibles (mismo apellido, mismo importe exacto), se ordenan por cercanía de fecha y se asignan de la más cercana a la más lejana, saltando las que ya tienen pareja. **La cercanía cuenta antes o después indistintamente** — el desfase entre que se registra el pago y el banco lo apunta va en las dos direcciones (decisión de la usuaria; la primera versión asumía que el banco siempre apunta después, y era falso).
  - **Y cuando varias encajan, decide la fecha, no la usuaria.** Antes se marcaba "ambiguo" y había que elegir a mano entre dos líneas del mismo nombre y el mismo importe, sin que la pantalla enseñara siquiera lo único que las distingue.
  - **El reparto vive en una sola función** (`repartirUnoAUno`). Estaba implementado dos veces —el cruce al subir y la lista de pagos sin emparejar— y discrepaban en 169 filas: la lista decía "elige cuál" de pagos que el cruce ya había cerrado solo.
- **2026-08-16 (noche)** — **El cruce ya no necesita el archivo.** Vivía dentro de la subida porque al principio los pagos no se guardaban: se leían, se cruzaban y se tiraban. Desde que se guardan todos, esa atadura no tenía sentido, pero la lista seguía diciendo *"le corresponde la línea X, vuelve a subir el archivo y se cerrará solo"* — mandaba a repetir una subida para que la app hiciera algo que ya podía hacer. Ahora **al abrir la pestaña se cruza y se escriben los enlaces**. Enlazar hace lo mismo que por los demás caminos porque usa la misma función: si la línea estaba sin resolver queda resuelta y con el proyecto del evento; si ya lo estaba, solo se escribe el enlace y no se le toca ni la nota ni el estado. La norma de un pago por línea se comprueba **contra la base** justo antes de escribir, no contra el reparto.
- **2026-08-16 (noche)** — **La app aprende cómo llama el banco a cada jugador.** El cruce compara el apellido de LarpManager contra el texto del banco, y hay nombres que no van a coincidir nunca: el banco escribe `Greer Matthias Rola` donde LarpManager pone `Matthias Greßer` — **se come la ß en vez de convertirla en ss**, así que no hay transliteración que lo arregle. Eso obligaba a vincular a mano sus cuatro pagos, uno a uno, y a repetirlo la vez siguiente. Era incoherente con el resto de la app, que aprende el proveedor de una línea y no vuelve a preguntar. Ahora, **al vincular un pago a mano se guarda cómo lo llama el banco** (tabla `larpmanager_alias`) y el reparto encaja por el apellido **o** por lo aprendido: se vincula uno de los cuatro y los otros tres se cierran solos.
  - **De dónde se aprende:** de la **columna del ordenante**, que es donde el banco escribe el nombre limpio. La primera versión miraba toda la línea y se quedaba con las palabras que aparecían en 6 líneas o menos — un número arbitrario que existía solo porque se estaba aprendiendo del sitio equivocado, y que además habría fallado justo con quien más pagos hace. La columna del ordenante **no se puede localizar por su rótulo** (en BBVA viene en blanco): si no aparece "BENEFICIARIO"/"ORDENANTE" en la cabecera, se coge la columna inmediatamente posterior a CONCEPTO cuando está vacía. El método viejo se queda como reserva para los movimientos importados antes de este cambio.
  - **Aviso: lo aprendido no se puede ver ni borrar desde la aplicación.** Si aprende algo mal, se queda.
- **2026-08-16 (noche)** — **LarpManager pasa a ser una pestaña propia** con el motor de tabla de siempre. Los pagos sin emparejar vivían en una ventana emergente con una tabla HTML suelta: sin ordenar, sin cambiar anchos, sin ocultar columnas y sin buscador — justo lo que la guía de diseño prohíbe. Ahora tiene ordenación por columna, anchos que se recuerdan, panel de columnas y buscador, igual que Movimientos y Facturas. Los dos botones de LarpManager se van de la barra de Movimientos a esa pestaña; **"Pagos sin emparejar" desaparece como botón** —la tabla es la página— y en su sitio queda **"Recalcular"**, para volver a cruzar sin salir y entrar. En Movimientos quedan Excel del banco, Devoluciones, Archivos subidos y Enviar a gestoría.
- **2026-08-16 (noche)** — **Método de trabajo, apuntado tras una tarde mala.** El trabajo de comparar dos excels fuera de la app acumuló ocho errores seguidos, todos encontrados por la usuaria y ninguno por comprobación propia: comparar el apodo en vez del apellido (por no leer `parsearCSV`, que ya lo resuelve), reescribir un lector de importes en vez de usar `lib/numero.cjs` (escrito ese mismo día), elegir columnas por su rótulo en vez de mirar el contenido —dos veces—, arrastrar el fallo de buscar trozos de texto en vez de palabras enteras que estaba documentado esa misma mañana, e inventar una explicación ("una comisión de 13,50 €") que no está en ningún dato y presentarla como hallazgo. **Las tres reglas que salen de ahí:** no rehacer lo que la app ya resuelve, mirar el contenido de las columnas y no su rótulo, y no rellenar un hueco de datos con una hipótesis. Y la de fondo: **si no hay forma de comprobar un resultado, eso se dice antes de entregarlo, no después.**
- **2026-08-18** — **El cruce de LarpManager propone, no escribe. Y por qué hubo que llegar a eso.** Durante unas horas el cruce enlazaba solo al abrir la pestaña. Dos casos reales de producción, encontrados por la usuaria leyendo la pantalla, obligaron a deshacerlo entero:
  - **`ALON` dentro de `ALONSO`.** El apellido se buscaba como trozo de texto, no como palabra. El pago de 150 € de Nitzan Alon se cerró contra *"TRANSFERENCIAS ANTON VIEJO ALONSO Registration fee 2 of Anton V"*: mismo importe, fecha cerca. Resultado: la línea se la quitó a su dueño, el pago de Antón salía pendiente teniendo su ingreso, y **el de Nitzan salía cobrado sin haberlo cobrado**. Arreglado exigiendo palabra entera. Barrido de los 220 enlaces vivos: 194 encajaban por palabra entera, 22 por alias, 3 a mano y **exactamente 1 por trozo — el falso**. Ningún emparejamiento bueno dependía de ello.
  - **`MISS` como si fuera un nombre.** Al vincular a mano se aprende cómo llama el banco a esa persona, y el camino automático reutilizaba esa misma función: **cada acierto falso se convertía en regla**. De la línea de Valeria Oliveira se aprendió "MISS"; con eso el cruce le dio el ingreso de Samantha van den Esschert (*"Miss Samantha Alexine Van Den Ess"*, mismo importe); y de ese enlace falso se aprendió que **"SAMANTHA" es como el banco llama a Valeria**. Un error alimentándose a sí mismo.
  - **La regla que sale de aquí: aprender solo cuando lo dice la usuaria.** La app propone en la columna Movimiento con la píldora de siempre y ella acepta o rechaza. Nada se escribe ni se aprende por su cuenta.
  - **Se rehizo todo el emparejamiento** (decisión suya): 220 enlaces deshechos, 193 movimientos devueltos a `sin_resolver` —los que solo estaban resueltos porque el cruce los había enlazado, sin nota ni factura propia—, 66 alias borrados. Los 6 pagos ignorados, intactos. Copia previa guardada antes de tocar nada.
- **2026-08-18** — **Un solo método para aprender, y qué se descarta.** Había dos y daban resultados distintos con el mismo enlace: uno leía la columna del ordenante, otro rebuscaba en la línea entera con un umbral de 6 apariciones puesto a ojo. Ahora hay **una regla: las palabras pegadas a su nombre** dentro del texto del movimiento (concepto + ordenante, si está). El banco escribe el nombre de quien paga como un bloque seguido y el resto es papeleo, así que anclarse en su nombre separa `VIEJO ALONSO` de `Registration fee 2 ticket 2027`. Se descartan además: tratamientos (Miss, Mrs, Herr…), nombres de otros jugadores, nombres de evento, cualquier cosa con dígitos, palabras de más de 14 letras (el apellido real más largo tiene 13) y las que salen por toda la lista. Si su nombre no aparece, no se aprende nada.
- **2026-08-18** — **La propuesta con dudas, en ámbar. Única excepción de color de toda la app.** Cuando el cruce no está seguro —el banco corta el apellido (*Van den Esschert* llega como `Van Den Ess`, *Kristiansen* como `Kris`), o el importe no cuadra— antes no ofrecía nada y había que buscar a mano entre todos los ingresos, aunque el propio "Por qué" acabara de nombrar el movimiento sospechoso. Ahora propone uno, el más cercano en fecha y siempre libre. Como una firme y una dudosa se aceptan con el mismo clic, tenían que distinguirse sin lugar a error: se maquetaron **cuatro opciones sobre datos reales** (borde discontinuo con la palabra "¿este?", borde solo, la palabra sola, y ámbar) y la usuaria eligió el ámbar viéndolas juntas. Vive en `--dudosa`/`--dudosa-soft` y solo se usa en `.sugerencia.dudosa`. En producción: 100 firmes, 13 dudosas, 27 sin propuesta.
- **2026-08-18** — **Rechazar es para siempre y libera el movimiento.** La ✕ guarda el **par pago–movimiento** (`larpmanager_rechazos`), no el pago: decir que no a una propuesta no es decir que ese pago no tenga movimiento, es decir que no es ese. Y como el reparto sigue buscando, rechazar hace que salga la siguiente mejor —o que ese movimiento pase a proponérsele a otro pago de la misma persona que lo estaba esperando.
- **2026-08-18** — **Estado del pago, columna Movimiento, e histórico de la persona.** Tres cosas que salieron de usar la pantalla de verdad:
  - **Estado `pendiente`/`resuelto`/`ignorar`**, mismo vocabulario que Movimientos, con su casilla "Solo pendientes". Nace de un problema concreto: para quitar un pago de la lista había que borrar el archivo entero y subir uno recortado. Borrar la fila no servía —se reconoce por su firma y volvería a entrar—, así que se marca y se queda.
  - **Columna Movimiento**: a qué está enlazado cada pago, con el concepto entero. Antes solo se leía dentro de la frase del "Por qué", en prosa, y no había forma de repasar los enlaces — que es exactamente como se descubrió lo de Alonso.
  - **Vincular a todo el ancho, con el histórico de esa persona encima.** Era un `<select>` dentro de una celda de 230px y el navegador cortaba el concepto justo por donde se distingue una línea de otra. El histórico trae todos sus pagos, incluidos los que no pasan por el banco (larpmoney, pasarelas), que no aparecen en ninguna otra pantalla y son justo los que explican por qué a alguien le faltan ingresos.
- **2026-08-18** — **La nota deja de ser obligatoria al confirmar con factura.** Se exigía solo en esa rama; confirmar a mano sin factura no la pedía. Era al revés de como tiene sentido: con el PDF adjunto hay más información, no menos. Y dejaba sin salida a una factura sin concepto —la nota se saca de ahí, y con una sugerencia pendiente ese campo se bloquea—, así que la app pedía algo que ella misma impedía escribir. La nota vacía se sigue aprendiendo como siempre: *"aquí no va nota"* es una respuesta más.
- **2026-08-18** — **Las combinaciones de facturas se ven también en Movimientos.** La sugerencia existía pero solo en la pestaña Facturas: desde Movimientos, una línea pendiente cuyo importe ya estaba explicado por dos facturas subidas parecía no tener nada. Ahora lleva su píldora en la columna Factura. Se leen los dos formatos en que está guardada esa sugerencia (el de ahora con lista, y el viejo con una sola factura): leer solo el nuevo habría cerrado la línea con una de las dos facturas, justo el error que la sugerencia existe para evitar.
- **2026-08-18** — **Una sola palabra: movimiento.** La app llamaba a lo mismo "línea del banco" y "movimiento" según la pantalla. Unificado en todo lo que se lee —Ayuda, mensajes de error, avisos de borrado, cabeceras y la columna "Por qué"—; los comentarios del código se quedan como están.
- **2026-08-19/20** — **"Por qué" pasa a ser una etiqueta, y por qué hacía falta.** Esa celda metía cuatro tipos de dato a la vez: el motivo, qué movimientos había relacionados, de quién era cada uno y qué hacer. Un párrafo de tres líneas por fila, ilegible, y contra la regla de la guía ("cada columna, un solo tipo de dato"). Queda la etiqueta: *Ok*, *El importe no cuadra*, *Solo coincide parte del nombre*, *Sin movimiento libre*, *No aparece en el banco*, *Emparejado*, *Ignorado a mano*, *Dado por bueno a mano*. El detalle vive en la columna Movimiento y en el panel de Vincular, que ya enseña todos los pagos de esa persona. Con eso se cayó solo un apaño del día anterior —esconder los movimientos que ya justificaban el pago de otra persona— que existía únicamente para que la frase no mintiera: a Paloma Vargas se le listaban las transferencias de Sara Albaladejo Vargas por compartir apellido.
- **2026-08-19/20** — **Enlazar un pago desde Movimientos, y las tres vueltas que costó.** Desde esa pantalla solo se podía deshacer el vínculo, nunca hacerlo; el caso que lo destapó fue una transferencia que el banco describe como *"Registration fee 2 of Calum"*, sin apellido, que el cruce no encuentra jamás. Se añadió un **Vincular** en la columna LarpManager que abre bajo la fila la lista de pagos sin movimiento, con buscador por nombre. Los pagos ya contestados (ignorados, dados por buenos) **se ven pero sin botón**: filtrarlos los hacía invisibles justo cuando hacen falta —si el ingreso es de alguien a quien ignoraste, verlo ahí es la única forma de darte cuenta.
  - **Lo que costó fue el sitio, no la lista.** El panel colgaba de la fila pero se anclaba al borde izquierdo de la *tabla*, y el botón está en la columna LarpManager, al final de una fila con desplazamiento horizontal: abrías algo que se quedaba fuera de la pantalla. Se probaron cinco maquetas del panel y una pantalla nueva de emparejar a dos columnas; **todas malas**, y la de dos columnas peor que lo que ya había (tarjetas apiladas, prohibidas por la guía, y duplicaba la pestaña LarpManager).
  - **La solución es la que ya estaba en la casa:** el contenido del panel se queda **pegado al borde de lo que se ve** (`position: sticky`), igual que las columnas Fecha y Concepto. Necesita medir el ancho visible del contenedor, porque un `100%` ahí dentro mide la tabla entera.
  - **Y de paso se quita la causa: Concepto deja de tener ancho fijo.** Todas las columnas eran píxeles clavados, así que la tabla medía siempre lo mismo y en cuanto la suma pasaba del ancho de la ventana había desplazamiento para siempre. Ahora Concepto se queda con lo que sobre: lo fijo suma 985 px, y en un contenedor de 1.368 el concepto pasa de 200 a unos 383. Si se arrastra a mano, el ancho arrastrado manda.
  - **Un intento fallido, anotado:** mover la columna LarpManager al lado de Concepto. Se subió sin aprobación, rompió la alineación de la cabecera de grupo (que seguía pintando esa celda al final) y se revirtió. Del intento sobrevive una sola cosa útil: esa celda parte el texto en vez de recortarlo con puntos suspensivos.
- **2026-08-25** — **El anticipo entra por su propia ventana, y dos campos de fecha huérfanos que reventaban la pantalla.** Un campo (`fechaPago`, del botón Pagar) apuntaba a una variable ya borrada del estado y lanzaba un `ReferenceError` en cada carga — la pantalla entera se veía rota, mostrando restos de dos renders a la vez, lo que se reportó como "aparecen dos formas de introducir anticipo". El otro (la fecha del propio anticipo) apuntaba a un campo que ya no existía en el estado: no rompía nada, pero era un campo muerto que nunca hacía nada. Los dos fuera — la fecha se pone sola con la de hoy en los dos casos, no hace falta pedirla. Y el formulario de anticipo pasa de estar siempre desplegado junto a un botón separado (dos formas de la misma acción, encontrado real) a la misma ventana emergente (`Modal`) que ya usan Proyectos y Colaboradores para "añadir algo nuevo" — una sola forma, siguiendo un patrón que ya existía en la app en vez de inventar uno nuevo.
- **2026-08-25** — **Separar y unir movimientos de un grupo, borrados del todo — la excepción que el 2026-08-15 dejó abierta, ya no hace falta.** Aquel día se quitó el botón pero se dejó el backend (`/separar`, `/unir` en `lib/agrupador.cjs`) "por si algún día hace falta separar líneas que el banco escribe idénticas". Medido hoy: nadie lo llamaba desde ningún sitio de la app, cero usos en 8 meses. Salen las dos rutas y las dos funciones.
- **2026-08-25** — **La X de descartar una sugerencia no se guardaba, y por qué no se notaba.** `/api/sugerencias/rechazar` tenía una lista de tipos permitidos que se había quedado corta (le faltaban `combo` y `pago`): la petición se mandaba, la API la rechazaba por dentro sin avisar con claridad, y la sugerencia volvía cada vez que se recargaba. Afectaba al combo de facturas (bug antiguo, nadie lo había notado porque las pruebas que existían llamaban a la función de la librería directamente, nunca a la ruta real) y a la propuesta de pago a colaboradores, añadida esa misma sesión. Arreglado añadiendo los dos tipos a la lista (`948c98f`), con una prueba nueva que pasa por la ruta HTTP de verdad en vez de por la función interna, para que este tipo de fallo no vuelva a pasar desapercibido.
- **2026-08-25** — **La propuesta de combinar facturas dice el proveedor y el importe de cada una, no solo el número.** Pedido explícito tras verla en la app real: con solo "facturas 101 + 25 + 10" no había forma de juzgar si la propuesta tenía sentido. Ahora cada factura sale como `número (importe€, proveedor)`, la suma total, y el concepto de la línea del banco entre comillas — en Movimientos (`6008163`) y, más tarde, en la propia pestaña Facturas donde nace el texto (`eae186f`, con `lib/textoCombo.cjs` nuevo para no duplicar la fórmula en los dos sitios). El dato ya estaba guardado en cada factura; el texto se compone al mostrarlo, no se recalcula nada.
- **2026-08-25** — **Empieza la red de pruebas que monta las pantallas de verdad, no solo la lógica de detrás.** Motivo concreto, no preventivo: un campo de fecha huérfano (`fechaPago`, borrado del estado pero olvidado en el JSX) reventó la pantalla de Cuentas entera, y nadie se enteró hasta que se vio rota en la app — build limpio, 65 pruebas de siempre en verde, ninguna tocaba una pantalla. Empieza por Movimientos (1.076 líneas, la que más cuesta tocar sin romper algo), con `@testing-library/react` + `happy-dom`, montando el componente de verdad y consultándolo por lo que se ve (roles, texto), nunca por estructura interna — para que sobreviva a mover código de sitio, que es justo lo que se piensa hacer después. Demostrado rompiendo la pantalla a propósito con el mismo tipo de fallo (`ReferenceError`) y viendo que la red lo pilla, dos veces, antes de darla por buena (`e380db2`). Extendida el mismo día a Cuentas y Facturas, las dos pantallas con más riesgo de las que quedaban sin cubrir (`c9191b9`): 28 pruebas en total entre las tres, sin tocar la base de datos ni la IA, ~2 segundos en correr.
  - **Coste: cero, y vigilado.** Leer una factura con la IA es lo que la app hace siempre que se sube una — con la clave puesta, cuesta dinero de verdad. Las pruebas nunca llegan ahí: solo tres sitios en todo el proyecto llaman a `analizarFactura` y las tres pasan siempre un lector de mentira; las pruebas de pantalla ni siquiera cargan ese módulo. `pruebas/preparar.js` además borra `ANTHROPIC_API_KEY` del entorno antes de correr nada, así que aunque una prueba futura se olvidara del lector, no llegaría a cobrar.
  - **Trampa encontrada, para no repetirla: la función `projects` de vitest, para tener dos grupos de pruebas (uno con base de datos, otro sin ella) en un solo comando, se bloqueó 58 minutos y siguió fallando.** Antes de eso, sin `projects`, la trastienda pasaba sola dos veces seguidas sin fallar nunca. La solución no fue seguir ajustando `projects`, sino dejar de usarla: `vitest.config.js` (la trastienda, casi igual que siempre) y `vitest.pantalla.config.js` (aparte, con `happy-dom` y un plugin propio para que Vite entienda JSX dentro de archivos `.js`), corridos como **dos comandos vitest distintos** en `npm test`, no como un único comando con dos grupos. **Si algún día algo tienta a unificarlos otra vez en `projects`, que se lea esto primero.**
  - **Estado de cobertura tras esto (medido, no a ojo): 3 de 14 componentes con red de pantalla** (Movimientos, Cuentas, Facturas — los de más riesgo), **0 de 9 páginas completas**, y de las 58 rutas de API, 5 se prueban llamando a la ruta HTTP real y unas 35 no tienen ningún rastro de su lógica en ninguna prueba. Quedan sin red, por orden de cuánto se usan: `PagosLarpManager.js`, `TablaColaboradores.js`, `GestionProyectos.js`, los tres formularios de subir factura, y `Ayuda.js`/`CabeceraApp.js`/`Modal.js`/`ConfirmDialog.js`/`ToastHost.js` — estos dos últimos con riesgo bajo porque ya se montan de forma indirecta dentro de las pruebas de hoy.
- **2026-08-25** — **Subir un excel del banco se quedaba cargando sin parar, en producción — causa encontrada: guardar uno a uno.** El archivo que lo disparó no tenía nada malo (cabeceras BBVA normales, formato ya conocido). Primer paso: `/api/excels` era la única ruta de las que hacen trabajo pesado sin `maxDuration` puesto (`facturas-generales`, `facturas` y `larpmanager` ya llevan 60 segundos) — puesto el mismo límite. Pero al volver a probar, la usuaria mandó la consola real del navegador: la petición sí terminaba — un 422 (normal: esta exportación se llama "Historico", no "bbva", hay que decir a qué banco pertenece) y luego, ya indicándolo, un 500. Reproducido en local pasando el archivo real por la función completa (`fusionarHoja`): **90 movimientos nuevos, guardados de uno en uno con un INSERT por fila, tardaban 24 segundos.** Cambiado a un único INSERT de varias filas a la vez (en tandas de 500, por si algún archivo trae miles de líneas) — el mismo archivo real pasa de 24 segundos a **3,2**. Mismos resultados exactos (90 filas, sin duplicados, comprobado contra la base de desarrollo). El límite de tiempo del punto anterior se queda puesto, de red por si acaso, pero ya no debería hacer falta.
